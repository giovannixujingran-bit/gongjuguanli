# 开发日志

> 按时间记录「哪天做了什么、动了哪些文件」的流水。**倒序排列，最新在最上面。**
> 与 [PROJECT_PLAN 决策记录](PROJECT_PLAN.md) 的分工：决策记录是「为什么这么定」的提炼清单（结论）；本日志是「做了什么」的过程流水。两者不重复——日志里涉及决策时，**引用决策记录编号**，不复述理由。
>
> **维护规则见 [CLAUDE.md](CLAUDE.md)：任何改动（文档或代码）都要在此追加一条。**

## 条目格式

```
## YYYY-MM-DD — 一句话标题
- 阶段：当前所处阶段
- 做了什么：要点（动词开头）
- 涉及文件：改/增/删了哪些
- 关联决策：决策记录 #n（如有）
```

---

## 2026-06-05 — 发布 v0.0.1 + 立版本发布规范 + 接 GitHub 远程

- **阶段**：发布工程（首个 tag）。
- **做了什么**：
  1. **立版本发布规范**：`CLAUDE.md` 新增「远程仓库与版本发布」一节——远程仓库 <https://github.com/giovannixujingran-bit/gongjuguanli>、SemVer（tag `vX.Y.Z`）、每 tag 对应 CHANGELOG 一个发布块、发版四步、并厘清「仓库版本号 ≠ 事件 `schema_version`」。
  2. **CHANGELOG 切发布块**：`## Unreleased` → `## v0.0.1 — 2026-06-05`，顶部留空 Unreleased。
  3. **接 GitHub 远程并首推**：`origin` = 上述仓库；远程已有 GitHub 占位「Initial commit」（仅 `# gongjuguanli` 一行 README），用 `merge --allow-unrelated-histories` 安全合并（不强推覆盖），并把占位 README 换成正式仓库落地页（指向 CLAUDE.md / PROJECT_PLAN / docs）。打带注释 tag `v0.0.1`。推送前核查 `.env`/`.pydeps`/缓存/`.run`/`.toolprefix` 均被 gitignore，无密钥或依赖误传。
  4. **代理**：本机 git 全局代理 7890 未运行，改用环境代理 17891（设为本仓库 `http(s).proxy`）方可访问 GitHub。
- **涉及文件**：改 `CLAUDE.md`、`CHANGELOG.md`、`开发日志.md`，新增/重写 `README.md`。
- **关联决策**：无新决策（发布工程）。

## 2026-06-05 — 补数据库迁移执行器（填「schema 可演进」缺口）

- **阶段**：Phase 2.5（接入工程化 / 标准化欠账收敛）。
- **背景**：架构审查发现一处与核心设计冲突的缺口——平台主打「schema 按 `schema_version` 演进、metadata 字段按信号晋升为主表列」，但存储层只有建表 SQL、没有「按顺序应用」的机制：`0001_init.sql` 仅靠 docker `initdb` 首次空库时执行，`0002+` 永不自动生效。真到第一次加列时改动落不到库。
- **做了什么**：
  1. **TDD 纯逻辑**：先写 `backend/storage/test_migrate.py`（看红）再写 `backend/storage/migrate.py`——`parse_migration_version` / `discover_migrations`（数字排序、重复编号报错、跳非迁移文件）/ `pending_migrations`（过滤已办）。6 个单测全绿。
  2. **IO 薄层**：`migrate.py` 加 `apply_pending`——建 `schema_migrations` 跟踪表、读已办、按编号顺序逐张应用，每张迁移连同登记放同一事务（半生效不可能）。按本项目惯例（同 `smoke_db.py`）IO 不进无 DB 的单测闸门，靠真库集成验证。
  3. **薄 CLI**：`tools/migrate.py`（同 `smoke_db.py` 款），`python tools/migrate.py` 一句话应用，默认读 `DATABASE_URL` 与 `backend/storage/migrations`。
  4. **真库验证**：本机 PG 用临时探针迁移跑两遍——第一遍应用 2 张（含 CREATE + ALTER ADD COLUMN）、第二遍幂等「无待办」、`schema_migrations` 正确登记，完事清理。另发现真实场景：旧库表是早先别的角色经 initdb 建的，直接重跑 `0001` 会因属主报错 → 故立「旧库先基线」规程写进 migrations/README。
  5. **全套机器闸门绿**：ruff format / ruff check / mypy strict（28 文件）/ vulture / import-linter（exit 0）/ pytest **26 passed**（原 20 + 新 6）。
  6. **文档同步**：`backend/storage/migrations/README.md` 重写（执行器用法 + 旧库基线 + 与 docker 关系）；`execution-plan.md` §五「迁移执行」行从待办改为已落地；`CHANGELOG` 记一条 Added。
  7. **接进 serve.ps1**：`serve.ps1 start` 在探活 DB 后、起 uvicorn 前自动跑 `tools/migrate.py`（失败即抛，不让 API 跑在过期库结构上）。本机旧库（表早先经 initdb/smoke 建）先基线一次（`INSERT schema_migrations (1,'0001_init')`），此后 `serve start` 自动迁移报「已是最新」。已 `serve.ps1 restart` 实测：迁移步骤执行 → API `/health` 就绪。
- **涉及文件**：新增 `backend/storage/migrate.py`、`backend/storage/test_migrate.py`、`tools/migrate.py`；改 `backend/storage/migrations/README.md`、`scripts/serve.ps1`、`docs/execution-plan.md`、`CHANGELOG.md`、`PROJECT_PLAN.md`（决策 #35）。
- **关联决策**：决策记录 #35（不引 Alembic、取仓库内最小幂等脚本，呼应 #27 离线务实路线）；填补 #33「字段晋升」依赖的库侧机制。

## 2026-06-05 — 修 POST /events 挂起根因 + 运行态加固（自愈 / 探活 / 登录自启）

- **阶段**：Phase 2C（运行态健壮性）。
- **根因**：`POST /events` 挂起不返回，实为 **PostgreSQL 未启动**（免安装 `D:\pg-portable` 非常驻，关终端即停）。`psycopg.connect` 默认无连接超时 → DB 不可达时无限等待 → 请求线程卡死、可连带拖垮线程池（`/health` 也会假死）。`/health` 不碰 DB 故仍正常，掩盖了问题。
- **做了什么**：
  1. **代码（正确性）**：新增统一连接入口 `backend/storage/db.connect`，带 `connect_timeout`（默认 5s，`DB_CONNECT_TIMEOUT` 可覆盖）；6 处 `psycopg.connect`（events / registry×2 / accounts×2 / smoke_db）全部改走它，删除多余 `import psycopg`。DB 不可达改为快速 500。**全套机器闸门绿**（ruff / mypy strict 27 文件 / vulture / import-linter / pytest 20 passed）。
  2. **脚本（可运维）**：新增 `scripts/pg.ps1`（启停免安装 PG，路径读 `.env` 的 `PG_BIN`/`PG_DATA`，缺省指向 `D:\pg-portable`）；升级 `scripts/serve.ps1`：`start` 时自动拉起 PG + **真实探活 DB**（TCP，连不上当场报错，不再假装起好）；新增 `scripts/autostart.ps1`（注册「登录自启」计划任务，已 install 并实测触发后 2s 内 `/health` ok）。
  3. **踩坑**：pg_ctl 起 PG 时若其 stdout 接进 PowerShell 管道，常驻 postgres 会继承管道写端永不关闭、读端等不到 EOF 而挂死。改为 `pg.ps1` 内用 `Start-Process + 文件重定向` 起 pg_ctl、`serve.ps1` 不再 `| Out-Host`。三个 `.ps1` 均存为 UTF-8 with BOM（PS5.1 否则按 GBK 误读中文）。
  4. **文档**：`docs/execution-plan.md` 新增 §四「本机运行（dev）」+ §五「已知非标准项 / 待收敛」（把原只在 AI 记忆里的偏离清单搬进仓库）；`.env.example` 补 `DB_CONNECT_TIMEOUT`/`PLATFORM_*`/`PG_*` 注释；`smoke.md` 前置指向 serve.ps1；`CHANGELOG` 记一条。
- **涉及文件**：新增 `backend/storage/db.py`、`scripts/pg.ps1`、`scripts/autostart.ps1`；改 `backend/storage/events.py`、`backend/storage/registry.py`、`backend/auth/accounts.py`、`tools/smoke_db.py`、`scripts/serve.ps1`、`.env.example`、`docs/execution-plan.md`、`docs/smoke.md`、`CHANGELOG.md`。
- **验证**：停 PG → 一条 `serve.ps1 start` 自动起 PG + API → `POST /events` `inserted:true`；登录自启任务触发后自动恢复。

## 2026-06-04 — 补全试点模板 pilot_tool / integration_version 形状定义

- **阶段**：Phase 2C（接入对齐，修模板缺口）。
- **背景**：aird-report 接入第 2 版反馈——`pilot_checklist.md` 强制要 4 个 metadata 键，但 `pilot_tool` / `integration_version` 从无形状定义（只有 `entry_source` / `auth_method` 定义过），接入方无从填。
- **做了什么**：在 `pilot_checklist.md` 给 4 键补上形状（`pilot_tool` = boolean 试点标记、`integration_version` = semver 埋点版本）；`tool_config.example.json` 补两键样例值（`true` / `"1.0.0"`）。两文件均不在 handoff-kit 内，无需重生成分发包。
- **涉及文件**：`platform/integrations/_template/pilot_checklist.md`、`platform/integrations/_template/tool_config.example.json`。

## 2026-06-04 — 审 aird-report 接入稿，登记 chapter_type metadata 约定

- **阶段**：Phase 2C 真实工具试点（接入对齐）。
- **做了什么**：
  - 用全套规范审 `平台用量上报-接入方案-aird-report.md`（工具方对齐稿），结论：契约层面通过，列出 3 处试点偏差（metadata 缺 auth_method/pilot_tool/integration_version、图片异步建议记 source cost、缺回滚开关）+ 回了对方 6 个问题。
  - **登记 `chapter_type` metadata 约定**（约定库新增「章节/段落类型」一行，2026-06-04 生效，首个登记方 aird-report）；属轻动作，**不升事件契约版本**（决策 #33 三层模型）。
  - 重跑 `export_integration_kit.py` 同步分发包。
- **涉及文件**：`platform/docs/metadata-conventions.md`（约定库 + 生效日期）、`platform/integrations/handoff-kit/*`（重生成）。
- **关联决策**：#33（metadata 约定属轻动作、不升契约版本）、#34（原文已放开，对方据此首轮即上报 output_content）。

## 2026-06-04 — 放开原文记录策略，读取侧权限留占位

- **阶段**：Phase 2C 起步（接入口径调整）。
- **做了什么**：拍板放开 `input_content` / `output_content` 原文记录（原「首轮不记原文」取消），写入侧不设门禁；仅读取侧可见范围 + 留存策略仍留占位待定。
- **涉及文件**：
  - 改 `platform/docs/schema.md`（敏感内容策略一节重写，去掉「待人工确认」标题与首轮不记）。
  - 改 `platform/docs/integration-guide.md`（边界表、§5 待定、发料口头交代、metadata 提示 4 处）。
  - 改 `platform/docs/metadata-conventions.md`（打字规则、文本产出行、第六节待定）。
  - 改 `platform/docs/contract.md`（敏感内容边界行）、`platform/docs/execution-plan.md`（Phase 2 §2C 概述 + 详细步骤 + 【不要做】占位行，共 3 处）。
  - 改 `platform/integrations/_template/pilot_checklist.md`（首轮采集范围）、`platform/integrations/_template/README.md`（试点采集范围说明）。
  - 改 `PROJECT_PLAN.md`（新增决策 #34、收尾 #33 尾句、下一步 2C 描述）、`CHANGELOG.md`（Unreleased/Changed）。
  - 改 `platform/tools/export_integration_kit.py`：`说明.txt` 模板里硬编码的旧口径「首轮不要上报原文/敏感策略未定」改为新口径（此处是策略的第二份副本，藏在生成器字符串里，首轮清理时漏扫——由 aird-report 接入稿 6.2 反查出）。
  - 重跑 `python platform/tools/export_integration_kit.py` 重生成 `platform/integrations/handoff-kit/`（生成物，非 SSOT）。
- **关联决策**：#34（放开原文记录、读取侧留占位）；承 #7 写入侧不鉴权、#17 读取侧挂靠账号表、#20 字段早为通用选填（故不升事件契约版本）。

## 2026-06-04 — 本机起真实服务、Phase 1.5 冒烟通过、登记首个真实工具 aird-report

- **阶段**：Phase 1.5 完成 + Phase 2C 起步（接第一个真实工具的前置就绪）。
- **关键发现**：`platform/.pydeps` 里**有 uvicorn 0.49**，用匹配的 codex-runtime Python 3.12 + `PYTHONPATH=.;.pydeps` 可起**真实 TCP 服务**（推翻此前「本机无 uvicorn、起不了服务」的判断）。PG16 免安装版已在 `D:\pg-portable` 运行、三表空库。
- **做了什么（运行手册，非仓库代码改动）**：
  1. 由 `.env.example` 生成 `platform/.env`（随机 `AUTH_TOKEN_SECRET`，已 gitignore 不入库）。
  2. `uvicorn backend.ingestion.app:app` 起在 `127.0.0.1:8000`，`/health` 正常。
  3. **Phase 1.5 真实 DB 冒烟通过**：`tools/smoke_db.py` 跑出 `模拟JSON → POST /events(202) → usage_event` 真实落库。
  4. `tools/seed_admin.py` 引导首个 admin（初始密码另存仓库外 `D:\pg-portable\admin-cred.txt`，待用户自改）。
  5. 经 `POST /registry/tools`（admin token）**登记首个真实工具 `aird-report`**（name=AI报告、team_id=aird、data_level=partial、collect_method=report）；DB 按码点校验 name 存储正确。
  6. 新增启停脚本 **`platform/scripts/serve.ps1`**（`start`/`stop`/`status`/`restart`/`logs`）：读 `.env`、自动探测 `.pydeps` 加 PYTHONPATH、PID/日志落 `.run/`（已 gitignore）。机器特定解释器路径放 `.env` 的 `PLATFORM_PYTHON`（不入库），提交的脚本保持通用。**坑**：Windows PowerShell 5.1 按 GBK 读无 BOM 的 .ps1 会把中文读乱致解析失败，故脚本存为 **UTF-8 with BOM**。
- **涉及文件**：platform/scripts/serve.ps1（新增）、.gitignore（加 `.run/`）；platform/.env、D:\pg-portable\admin-cred.txt 为本机产物不入库。
- **未做**：AI报告 工具侧 SDK 接入代码（在工具自有仓库，不在本仓库）；relay、敏感策略、ROI 仍待定。
- **运行态说明**：uvicorn 为前台 dev-run（后台进程），非常驻服务；长期应上 Dockerfile/服务化（见记忆 platform-nonstandard-points）。
- **关联决策**：#29（接入顺序）、#31（admin 门禁）、#32（tool_id 发放通道）。

## 2026-06-04 — 新增《metadata 语义约定与字段治理》：三层模型 + 约定库 + 信号触发晋升

- **阶段**：接入文档收口（字段扩展治理）。
- **背景**：用户连串追问——接入方要记没预置的字段由谁定、metadata 会不会乱、适不适合长期管理、以后好不好搬家。结论需成文：metadata 不是「完全自由」，要有规范；也不是「长期的家」，是带约定的缓冲 + 晋升候选池。
- **做了什么**：新增 `platform/docs/metadata-conventions.md`，作为「metadata 怎么记 + 字段三层治理 + 晋升流程」的 SSOT。内容：① 字段三层模型（主表统一字段/metadata 约定/纯一次性），并厘清「要不要记（看有没有用，与频率无关，全记）」与「记在哪（频率决定 metadata 还是列）」是两件事、「不晋升≠不记录」；② 打字规则（snake_case、时间用 number 毫秒、二进制只存引用不入库、原文走主字段受敏感策略）；③ 约定库初始几类（分段耗时/通用产出物/图片/文件/文本/分类维度/错误详情）；④ 申请扩展流程（查约定库→找平台→给新约定[轻，不升事件版本] 或 提主表列[重，升版本]，信号触发非定期）；⑤ 晋升 metadata→列的步骤与注意（加列+回填 UPDATE、对接入方透明、历史脏数据是最大风险，故打字规则=好搬家的保险）；⑥ 待定项（metadata 不合约定是否告警、敏感策略）。
- **守 CLAUDE.md 规范（连带全做）**：PROJECT_PLAN 文档地图加行 + 决策记录 #33；CLAUDE.md §1 结构树 + SSOT 铁律清单 + §3 传播表（新增「改 metadata 约定→只改该文档不升版本；晋升→走四连动」一行）；接入指南 §2 加指向；`export_integration_kit.py` 把它纳入分发包（`metadata约定.md`）并更新说明，重跑生成器。
- **验证**：脚本过闸门（ruff/mypy 24 files Success/format）；分发包 7 文件含 `metadata约定.md`；新文档 6 处跨文档链接全部验证可达。
- **涉及文件**：新增 `platform/docs/metadata-conventions.md`；改 `PROJECT_PLAN.md`、`CLAUDE.md`、`platform/docs/integration-guide.md`、`platform/tools/export_integration_kit.py`、`CHANGELOG.md`；重生成 `platform/integrations/handoff-kit/*`。
- **关联决策**：#4（不设列进 metadata 的先例）、#18（不阻断入库）、#33（本次字段治理三层模型）。

---

## 2026-06-04 — 接入指南补「分工边界」小节：对方只上报，不必自建管理/账号/统计

- **阶段**：接入文档收口。
- **背景**：用户问"人员要不要同步、对方是不是也要做个管理系统"。这是接入方最易过度开发的地方——指南原先没明说边界。
- **做了什么**：`platform/docs/integration-guide.md` 在开头加「你和平台的分工」小节（不编号，避免打乱 §0–§5 与 §2/§3 引用）：明确①身份唯一来源是平台账号体系，人员**不用同步名单**（首轮 anonymous，后续走门户注入或平台 Auth，非导入对方用户表）；②对方**不必自建**用户管理/统计看板/工具注册表，平台已集中做，各建一套违背"统一汇总"初衷；③例外：对方原有登录系统自用保留，对接是复用平台身份而非新建。重跑 `export_integration_kit.py` 同步分发包。
- **守 SSOT**：本小节是对既有决策（#16 入口门户、#17 账号体系、#29 portal/direct 入口）的接入向转述，权威仍在 architecture.md / PROJECT_PLAN 决策记录，不新增决策。
- **涉及文件**：改 `platform/docs/integration-guide.md`；重生成 `platform/integrations/handoff-kit/*`。
- **关联决策**：#16 / #17 / #29。

---

## 2026-06-04 — 接入资料分发包改为「脚本生成、入仓库」，立 CLAUDE.md 同步规范

- **阶段**：接入文档收口（分发副本治理）。
- **背景**：上一步我把接入资料拷成桌面文件夹（仓库外快照），用户指出问题对——仓库外手抄副本会随源文档改动而失效，正是 SSOT 最忌的「同一内容两处、改一处忘另一处」，且应有维护规范。
- **改法（按 shared/contracts 生成物的既有套路）**：
  - 删除桌面外部副本。
  - 新增 `platform/tools/export_integration_kit.py`：从 SSOT 文档（`docs/integration-guide.md` / `docs/contract.md` / `docs/schema.md` / `shared/schema/event.schema.json`）生成分发包到**仓库内** `platform/integrations/handoff-kit/`（中文文件名 + 说明.txt + README；.md 顶部加「自动生成」横幅指回源；版本号取自 `shared/schema_version.py`）。本脚本只复制不重写，分发包非 SSOT。
  - 立规范：CLAUDE.md §3 新增传播行「改接入文档 → 重跑 export_integration_kit」、§1 结构注明 handoff-kit 是生成物勿手改；`.pre-commit-config.yaml` 把 handoff-kit 加入 prettier exclude（同 shared/contracts，防格式化改动生成副本）。
- **验证**：脚本过机器闸门（ruff/mypy 24 files Success/vulture/pytest 20 passed）；生成包 6 文件（接入指南/接入契约/字段文档/event.schema.json/说明.txt/README.md），契约 v0.2。
- **涉及文件**：新增 `platform/tools/export_integration_kit.py` + 生成的 `platform/integrations/handoff-kit/*`；改 `CLAUDE.md`、`.pre-commit-config.yaml`、`CHANGELOG.md`。
- **关联决策**：呼应 SSOT 铁律（CLAUDE.md §1）；分发包对待方式同决策 #27 的生成物理念。

---

## 2026-06-04 — 新增《接入指南》：给接入方的总入口（可直接发给外部团队）

- **阶段**：接入文档收口（配合多方协作）。
- **背景**：现有接入资料（contract.md / schema.md / event.schema.json / SDK / _template）齐全但散着，没有一个"递给对方、照着就能改"的总入口。用户要一份可直接发出的文档。
- **做了什么**：新增 `platform/docs/integration-guide.md`——五步接入流程（领 tool_id → 读契约 → 按 schema 构造 → 上报 → 自检验收）、一个最小可跑示例 payload（圈一 + 入口来源，LLM 再补圈二）、`POST /events` 上报方式与身份（portal/direct/unknown）、"在工具代码里改什么"（失败/流式/异步义务摘要）、当前边界（首轮不记原文、relay 未提供、自助校验 CLI 规划中）、以及"把哪些文件一起发给对方"清单。
- **守 SSOT**：指南只做"快速上手 + 示例 + 边界"，字段定义链 [schema.md](platform/docs/schema.md)/[event.schema.json](platform/shared/schema/event.schema.json)、义务细则链 [contract.md](platform/docs/contract.md)，不复制正文。中文锚点改为纯文本 §N 指代。
- **文档治理**：PROJECT_PLAN 文档地图 + CLAUDE.md §1 结构各加一行（接入指南＝先读、contract＝细则）。
- **涉及文件**：新增 `platform/docs/integration-guide.md`；改 `PROJECT_PLAN.md`、`CLAUDE.md`、`CHANGELOG.md`。
- **关联决策**：#19（工具自报为主路）、#29（portal/direct 入口）、#32（tool_id 发放通道，指南引为第一步）。

---

## 2026-06-04 — tool_id 发放通道落地：POST /registry/tools 管理员 API（多方协作第一公里）

- **阶段**：Phase 2.5 接入工程化（决策 #32），打通同事接入的第一步＝领 `tool_id`。
- **背景**：复查指出 `tool_id` 只能平台方手工 `INSERT`、注册表无发放入口，是多方协作最先撞上的断点；命名规则也长期「待团队端确认」。用户拍板：通道取「更符合工程规范」的形态、命名用 `<team>-<tool>`。
- **设计选择**：发放通道做成**管理员 API** `POST /registry/tools`（与 `/auth/users` 同门禁 #31），而非发 DB 凭据的离线 CLI——平台已有 FastAPI + admin token，注册走同一套 REST + 鉴权才是规范，离线 seed 脚本只用于引导首个 admin。命名规则定稿 `<team>-<tool>`（详见决策 #32）。
- **实现（镜像现有 accounts.py / events.py 写法，TDD）**：
  - 新增存储层 `backend/storage/registry.py`：`ToolRegistryRepository` Protocol + `PostgresToolRegistryRepository`（`INSERT ... ON CONFLICT (tool_id) DO NOTHING RETURNING`，无返回＝主键冲突 → 抛 `ToolAlreadyExistsError`）；命名规则 SSOT 常量 `TOOL_ID_REGEX`。
  - 接入层 `backend/ingestion/app.py` 新增 `POST /registry/tools`：请求模型用 `Field(pattern=TOOL_ID_REGEX)` 自动校验命名（非法 422）、`require_admin` 门禁、重复 409；新增 `get_tool_registry_repository` 依赖、`ToolRegisterRequest`/`ToolPublic`/`public_tool`。MVP 只发接入字段，门户展示字段后续设。
  - 测试 `backend/ingestion/test_app.py`：新增 `MemoryToolRegistryRepository` + 4 条（合法 201 + 默认值、需 admin 401/403、重复 409、非法命名 422 多样例）。
- **验证（全部真跑）**：✅ 全套机器闸门 CI-exact 绿——ruff/ruff format/mypy（23 files, Success）/vulture/import-linter/**pytest 20 passed**（原 16 + 新 4）。✅ **真库冒烟**（进程内 ASGI + 本机 PG16）：真实建 admin → 登录 → 注册 201 + `tool_registry` 真实落行（默认 `minimal`/`report`/`enabled=t`、中文 `name` UTF-8 正确）→ 重复 409 → 非法命名 422 → 无 admin 401，独立 psql 复核一致；冒烟测试行已清理（三表归零）。临时脚本 `D:\pg-portable\registry_smoke.py`，未入库。
- **文档同步（SSOT）**：`registry.md` 命名规则定稿 + 新增「§三 发放通道」；`execution-plan.md` Phase 2.5 该项标 ✅ 已实现；`contract.md` 登记说明改指向发放通道；`PROJECT_PLAN` 决策 #32 + 「下一步」更新。
- **涉及文件**：新增 `backend/storage/registry.py`；改 `backend/ingestion/app.py`、`backend/ingestion/test_app.py`、`platform/docs/registry.md`、`platform/docs/execution-plan.md`、`platform/docs/contract.md`、`PROJECT_PLAN.md`、`CHANGELOG.md`。
- **关联决策**：#9（tool_id 平台统一分配 → 需注册表）、#31（admin 门禁模式）、#32（本次：通道形态 + 命名定稿）。

---

## 2026-06-04 — 真实数据库冒烟通过：本机装免安装 PG16，JSON→/events→usage_event 真实落库 + 幂等验证

- **阶段**：Phase 1.5 真实数据库冒烟（决策 #29 列为真实工具接入的第一步），首次真正跑通。
- **背景**：此前因本机无 Docker / PostgreSQL，真实落库链路一直没验过（只验了机器闸门）。本轮用户授权在本机装 PG。
- **装了什么（绿色版，可整目录删除卸载）**：下载 EDB 的 **PostgreSQL 16.14 免安装二进制 zip**（对齐 docker-compose 的 `postgres:16`）解压到 `D:\pg-portable\`（**不在仓库内，不入库**）；`initdb` 建数据目录 `D:\pg-portable\data`（trust 本地认证、UTF8），`pg_ctl` 起在 5432；建角色 `platform` + 库 `platform`、跑 `backend/storage/migrations/0001_init.sql` 建三张表并授权。
- **怎么跑的冒烟**：本机无 uvicorn 且 PyPI 被墙（SSL EOF）装不了，故没起 TCP 服务，改用 `httpx.ASGITransport` 在进程内驱动**真实 FastAPI 应用**——真实 `/events` 路由 + 真实 `UsageEvent` 校验 + 真实 `normalize_event`（user_id→anonymous）+ 真实 psycopg INSERT 进**真实 Postgres**，唯一省掉的是 TCP socket 那一跳（对"落库链路通不通"无关紧要）。临时脚本 `D:\pg-portable\smoke_inproc.py`，复用 `tools/smoke_db.py` 的 payload/查库逻辑，**未入库**。
- **结果（真跑、独立 psql 复核）**：第一次 POST → 202 `inserted=true`；同 `record_id` 再发 → 202 `inserted=false`（**幂等成立**，`ON CONFLICT DO NOTHING` 真实生效）；库内仅 **1 行**，`schema_version=v0.2`、`tool_id=smoke-demo-tool`、`user_id=anonymous`、`status=success`、`metadata` JSONB（entry_source/auth_method）均正确持久化。用 `platform` 角色独立 `psql` 查询复核一致。
- **遗留/边界**：① PG 仍在本机 5432 运行，用户用完可 `pg_ctl ... stop` 后删 `D:\pg-portable\`；② 真实 TCP 服务（uvicorn）这一跳本机跑不了（无 uvicorn + PyPI 被墙），到能联网/有 uvicorn 的机器可按 `smoke.md` 原样跑一遍；③ 连接池缺失等既有边界不变（见 [[platform-nonstandard-points]]）。
- **涉及文件**：无仓库内代码改动（仅本机环境 + 仓库外临时脚本）。
- **关联决策**：决策记录 #10（幂等去重）、#18（不阻断入库 / 匿名兜底）、#29（真实接入第一步＝真实库冒烟）。

---

## 2026-06-04 — 通电验证：跑通全套机器闸门，修复一直误报为绿的 mypy 红灯

- **阶段**：Phase 2A/2B 真实冒烟前的"通电"验证（按用户"先验证"指示）。
- **背景**：此前多轮日志称机器闸门"绿、仅生成文件有报错可忽略"，但 pytest/import-linter 因本机 Python 3.13 与 vendored `.pydeps`（cp312）ABI 不匹配**从未真正在本机跑过**。本轮目标：先把闸门真正跑起来。
- **解决环境断层**：在 `.tool-venv` 的 base 找到一个 **Python 3.12.13** 解释器（`C:\Users\82490\.cache\codex-runtimes\codex-primary-runtime\dependencies\python\python.exe`），正是 `.pydeps`/`.toolprefix` 编译目标。用它 + `PYTHONPATH=.pydeps`（ruff 二进制在 `.toolprefix\Scripts\ruff.exe`）即可在本机跑全套闸门，无需另装 3.12。
- **抓到真红灯**：CI-exact 的 `mypy backend collection tools` **实际 exit 1**——`exclude=["shared/contracts/"]` 对"被业务代码 import 而 follow-imports 追进去"的生成文件**不生效**，故生成文件里 4 处 `字段: 类型 = ...`（裸 Ellipsis）被 mypy strict 判为类型不兼容。之前几轮把它当"可忽略"是误判，闸门一直是红的。
- **修复（改生成器，不手改生成文件）**：`tools/generate_contracts.py` 对"必填且无额外约束"的字段改吐 `= Field(...)` 而非裸 `= ...`（运行时等价，mypy 放行）；重跑生成器重生成 `shared/contracts/event_model.py`。
- **验证（全部真跑、CI-exact）**：✅ ruff format（22 files formatted）、✅ ruff check（All checks passed）、✅ mypy（Success: no issues found in 22 source files）、✅ vulture、✅ import-linter、✅ pytest（**16 passed**，含上轮新增的 auth 门禁两条测试，首次真实通过）。
- **当时未能做、随后已补做**：真实 PostgreSQL 冒烟——见下一条「真实数据库冒烟通过」（同日在本机装了免安装 PG16 后完成）。
- **涉及文件**：改 `tools/generate_contracts.py`；重生成 `shared/contracts/event_model.py`（`event.d.ts` 内容不变）。
- **关联决策**：决策记录 #27（生成器为仓库内本地生成器，此次只动生成逻辑不动数据契约）。

- **阶段**：文档结构收尾（决策 #30）。
- **背景**：用户要求「标准、可长期维护、可交接」的结构，且不再要「2 份看 platform、5 份看规划」的双轨。故执行决策 #26 的 (a) 升级为**全量迁移**。
- **做了什么**：
  - 用 `git mv` 把 5 份规范搬入 `platform/docs/` 并统一英文名：架构与原则→`architecture.md`、执行计划→`execution-plan.md`、代码规范→`code-standards.md`、工具注册表→`registry.md`、工具门户→`portal.md`；mockup 搬到 `platform/docs/mockups/`；两份冻结归档（数据契约/接入契约，真源早在 platform）`git rm`；删除空的 `规划/` 目录。
  - 修正全仓指向 `规划/` 的跨文档链接：7 份 docs 内部互链改同级文件名；CLAUDE.md（§1 结构树、SSOT 段、§2/§3 表、§4/§5）、PROJECT_PLAN（入口说明 + 文档地图 + 决策链接）、platform/README、docs/README、各子目录 README、pyproject/pre-commit/ci/gen-models 注释、event.schema.json 描述。历史 `开发日志.md` 旧条目作为时间账本保留原样、不改写。
  - PROJECT_PLAN 追加决策 #30（全量迁移）、#31（/auth/users admin 门禁）。
- **过程事故与恢复（如实记录）**：①最初图快用 `sed` 批量改链接，但 C locale 下 `.` 按字节匹配，误伤了正文里多字节 `规划/` 路径 → 已用 Python 按字面/`[^)]*` 重写 schema.md/contract.md banner 修复；②`git mv` 对「工作树已修改但未提交」的 `执行计划.md`/`架构与原则.md` staged 的是 HEAD 旧 blob，随后 `git checkout` 把未提交的 Phase 1.5/2A-2D/2.5、决策#29 等内容覆盖丢失 → 已从会话上下文 + git 对象库核对后**完整重建** execution-plan.md、architecture.md §五、registry.md 命名段。**教训：移动有未提交改动的文件前应先 commit。**
- **验证**：全仓 grep 确认无残留 `](规划/...)` 活链接（仅历史 开发日志 prose 保留）；7 份 docs 与改动文件均 UTF-8 正常解码。代码未改，无需跑机器闸门。
- **关联决策**：#26（原部分冻结）、#30（升级为全量迁移）。

## 2026-06-04 — 收紧账号创建：/auth/users 加 admin 门禁 + 首个 admin 走 seed 脚本

- **阶段**：Phase 2B 安全收口（读取侧权限挂靠点）。
- **背景**：局域网威胁模型下不做写入侧鉴权（事件上报）成立，但**账号体系是读取侧权限（谁能看 input/output 原文）的挂靠点**（决策 #17）。原 `/auth/users` 完全开放且可直接造 admin，等于任何能访问 API 的人都能给自己开 admin、架空将来的读取侧分级。故收紧。
- **做了什么**：
  - `backend/ingestion/app.py` 新增 `require_admin` 依赖，`POST /auth/users` 改为**需 admin token**；非 admin 403、无 token 401。
  - 新增 `tools/seed_admin.py`：首个 admin 无法经端点创建，由运维用 DB 凭据离线引导一次。
  - 更新 `backend/auth/README.md` 说明门禁与引导方式；新增/改写两条接入测试（admin 建用户闭环、非 admin/无 token 被拒）。
- **验证**：ruff / ruff format / vulture 通过；mypy 对改动文件无错（报错均在被 exclude 的生成文件）。pytest 仍因本机 Python 3.13 与 .pydeps（cp312）ABI 不匹配未跑，需 3.12 环境补跑（新增的两条 auth 测试待补跑）。
- **关联决策**：决策记录 #7（写入侧不鉴权的边界）、#17（账号体系=读取侧权限挂靠点）、#31（本次收紧入账）。

## 2026-06-04 — 整体复查落地（多方协作视角）：契约版本单一来源 + SDK 自愈重试 + 接入工程化规划

- **阶段**：Phase 2A/2B 复查后整改（真实冒烟 / 试点前）。
- **背景**：按用户对整体体检各点的逐条意见执行（命名按团队划分先占位、契约校验写进规划、版本/重试直接改、运维边界直接做、relay 待确认想法、ROI 先不碰只保证兼容）。已核对项目框架＝局域网单点部署、配置走环境变量，确认「分发副本泄露」风险不适用。
- **直接实现**：
  - 新增 `shared/schema_version.py` 作为契约版本单一来源（`CURRENT_SCHEMA_VERSION` / `SUPPORTED_SCHEMA_VERSIONS`）；`collection/sdk/tracker.py` 与 `tools/smoke_db.py` 改为引用常量，不再硬编码 `"v0.2"`。
  - 接入层 `backend/ingestion/app.py` 新增 `warn_if_unsupported_schema_version`：未知版本仍照收（守「不阻断入库」#18），但记告警日志。
  - SDK 重试改自愈：`report()` 成功后自动 `flush()` 重发积压 buffer；buffer 读写加 `threading.Lock`。补齐决策 #10 承诺的「重试队列」语义（此前只有手动 flush）。
  - 接入模板 buffer 路径按工具隔离（含 `tool_id`），`pilot_checklist.md` 补说明。
- **写进规划**：
  - `规划/总则/执行计划.md` 新增 **Phase 2.5 接入工程化**：自助契约校验 CLI、`tool_id` 发放通道、接入层连接池（三者均「待实现」）。
  - `规划/共享层/工具注册表.md` 补 `tool_id` 命名约定：按团队分段、规则由团队端设定、先用占位/模板（待团队端确认）。
  - `platform/docs/contract.md` §一 引一笔「自助校验（规划中）」。
- **relay（按用户指示「先确认想法再写」）**：用户拍板「先写设计稿待确认再实现」+「透明代理为主 + 旁路记录兜底，两者都要」。已把设计稿写进 [platform/collection/relay/README.md](../platform/collection/relay/README.md)（标「设计稿·待确认」，列出 4 个待拍板点：支持的 provider 形态 / 流式是否进首版 / key→tool_id 映射存哪 / OpenClaw 对接方式），执行计划 Phase 2D 指向它。**未写实现代码**，待确认。ROI 算式按用户指示先不碰，仅保持 schema/存储对任意将来数据可兼容（现有弹性选填 + metadata 已满足）。
- **验证**：ruff、ruff format、vulture（无死代码）通过；mypy 对改动文件无错（报错均在被 exclude 的生成文件 `shared/contracts/`）。import-linter 与 pytest 因 `.toolprefix`/`.pydeps` 为 Python 3.12 编译（`_rustgrimp`/`_pydantic_core` 与本机 3.13 ABI 不匹配）**未能在本机运行**；分层方向经静态确认只新增「上层→shared」合法依赖，无反向/跨层。需到 3.12 环境补跑 import-linter + pytest。
- **关联决策**：决策记录 #10 / #18 / #9（注册表/命名）/ #12（ROI 占位）。

## 2026-06-04 — 逻辑梳理复查：修正易误导处 + 澄清已知缺口

- **阶段**：Phase 2A/2B 复查（真实冒烟 / 试点前）。
- **做了什么**：复查数据传输 / 存储 / 接入配合三方面，修两处易误导实害、澄清若干缺口：
  - SDK 把 `record_usage(async_report=)` 参数与 `report_async()` 方法改名为 `background_send` / `send_in_background`，并加注释——它只是「后台线程上报」，与契约 §四「异步任务两段式」无关，避免接入方混淆。
  - 建表 SQL `tool_id` 注释由误导性的「FK → tool_registry」改为「逻辑引用，不建强制外键」，并说明理由（建外键会拒收未登记工具，违背「不阻断入库」#7/#18）。
  - 接入契约 §五补 relay 现状说明：relay 属 Phase 2D，截至 2A/2B **尚未实现**，当前试点只接「能改代码、走工具自报」的工具。
  - schema.md 把 `result_quality` 由「float/enum」收敛为「float」，并提示：契约校验放行字符串但落库列是 `REAL`，非数值会被静默丢成 NULL，enum 形态质量应放 `metadata`（与 storage `numeric_quality` 行为对齐）。
- **验证**：ruff lint/format 通过；mypy 对改动文件 `tracker.py` 无类型错误（报错均在被 exclude 的生成文件 `shared/contracts/`）。pytest **未在本机跑通**——`.pydeps` 为 Python 3.12 安装（`_pydantic_core.cp312` 扩展），本机仅 Python 3.13，ABI 不匹配，需到 3.12 环境补跑。
- **未改（记为已知缺口，留待后续）**：① SDK 决策 #10 承诺的「重试队列」目前只有手动 `flush()`、无自动后台重发；② 存储层每请求新建连接、无连接池。二者真实试点放量前需处理。
- **关联决策**：决策记录 #7 / #10 / #18 / #29。

## 2026-06-03 — 实现 Phase 1.5/2A/2B，并补 Phase 2C 真实工具试点模板

- **阶段**：Phase 2A/2B 最小实现，Phase 2C 试点准备。
- **做了什么**：
  - 新增真实数据库冒烟脚本与说明，用于验证 `模拟 JSON → /events → usage_event`；当前机器无 Docker 时需在具备 PostgreSQL 的环境补跑。
  - 实现 `collection/sdk` 参考 SDK：事件构造、异步上报、本地缓冲、重试、token 归一化，以及 `entry_source` / `auth_method` metadata。
  - 新增 demo 工具，可模拟 `portal` / `direct` / `unknown` 三种入口。
  - 实现统一 Auth API 最小版：创建用户、登录、token 校验、`/auth/me`；`/events` 带合法 token 时由平台解析身份。
  - 新增 `integrations/_template` 真实工具试点清单和配置样例，明确首轮不记录原文、保留回滚开关。
  - 同步 README、执行计划、CLAUDE.md 与 CHANGELOG 的当前阶段。
- **涉及文件**：.gitignore、platform/tools/smoke_db.py、platform/docs/smoke.md、platform/collection/sdk/*、platform/backend/auth/*、platform/backend/ingestion/*、platform/integrations/_template/*、platform/README.md、platform/backend/auth/README.md、platform/pyproject.toml、platform/.env.example、规划/总则/执行计划.md、CLAUDE.md、CHANGELOG.md、开发日志.md。
- **关联决策**：决策记录 #29。

## 2026-06-03 — 调整下一步计划：统一用户系统支持 portal / direct 两类入口

- **阶段**：Phase 2 规划调整。
- **做了什么**：
  - 更新 [架构与原则](规划/总则/架构与原则.md)：统一用户系统不只服务门户，也可通过 Auth API / SDK 被外部工具复用；入口分 `portal` / `direct` / `unknown`，识别不了仍 `anonymous`。
  - 更新 [执行计划](规划/总则/执行计划.md)：新增 Phase 1.5 真实数据库冒烟；Phase 2 拆为 SDK + demo 工具、Auth API 最小版、真实低风险工具试点、本地 relay 兜底。
  - 更新 PROJECT_PLAN 决策 #29 与下一步；CHANGELOG 的 Unreleased 记录 Phase 2 计划变化。
  - 明确 `entry_source` / `auth_method` 先写入 metadata，暂不升 schema，等验证稳定后再评估 v0.3。
- **涉及文件**：规划/总则/架构与原则.md、规划/总则/执行计划.md、PROJECT_PLAN.md、CHANGELOG.md、CLAUDE.md、开发日志.md。
- **关联决策**：决策记录 #29。

## 2026-06-03 — 新增 CHANGELOG，并把维护规则写入 CLAUDE.md

- **阶段**：Phase 1 收尾文档治理。
- **做了什么**：
  - 新增 [CHANGELOG](CHANGELOG.md)，记录 Phase 0 地基与 Phase 1 最小后端闭环的阶段级能力变化。
  - 更新 CLAUDE.md：定义 `开发日志.md` 与 `CHANGELOG.md` 的分工、什么时候必须更新 CHANGELOG、改完自检清单。
  - 更新 PROJECT_PLAN 文档地图与决策记录 #28。
- **涉及文件**：CHANGELOG.md、CLAUDE.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #28。

## 2026-06-03 — Phase 1：打通模拟事件上报到后端存储闭环

- **阶段**：Phase 1（存储层 + 接入 API）。
- **做了什么**：
  - 将 `platform/scripts/gen-models.{ps1,sh}` 改为调用仓库内本地生成器 `platform/tools/generate_contracts.py`，从 `event.schema.json` 生成 Pydantic v2 模型与 TS 类型，避免依赖全局 `uv` / 联网 `npx`，并同步代码规范与决策 #27。
  - 实现接入 API：`backend/ingestion/app.py` 提供 `/health` 与 `/events`，用生成模型校验模拟事件，缺圈一字段返回 422；`user_id` 缺失补 `anonymous`；`ingested_at` 由服务端写入；按 `record_id` 幂等返回 inserted 状态。
  - 实现存储层：`backend/storage/events.py` 提供 PostgreSQL `INSERT ... ON CONFLICT DO NOTHING` 落库逻辑，metadata 走 JSONB，DB 地址走 `DATABASE_URL`。
  - 实现账号最小支撑：`backend/auth/passwords.py` 用 PBKDF2-SHA256 做密码哈希/校验，不存明文。
  - 补测试：模拟数据成功上报、缺必填拒收、重复上报幂等、密码哈希、存储参数转换。
  - 拧紧后端机器闸门：mypy strict、vulture、import-linter、pytest 在 CI 必过；前端仍占位宽松。
- **涉及文件**：platform/backend/ingestion/*、platform/backend/storage/*、platform/backend/auth/*、platform/shared/contracts/*、platform/tools/generate_contracts.py、platform/scripts/gen-models.*、platform/pyproject.toml、platform/docker-compose.yml、.github/workflows/ci.yml、.pre-commit-config.yaml、platform/README.md、规划/总则/代码规范.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #1–#6、#13、#14、#26、#27。

## 2026-06-03 — Phase 0 收尾：统一部分冻结表述，交接命令切到 Phase 1

- **阶段**：Phase 0 收尾，准备进入 Phase 1。
- **做了什么**：
  - 修正 `platform/docs/schema.md`、`platform/README.md`、`platform/shared/schema/event.schema.json` 中关于 SSOT / 冻结状态的表述，统一为「数据契约 + 接入契约部分冻结，SSOT 已转至 `platform/`；其余 5 份仍活在 `规划/`」。
  - 更新 [规划/总则/执行计划](规划/总则/执行计划.md) 的当前进度与交接命令：Phase 0 已完成，下一步进入 Phase 1（存储层 + 接入 API）。
  - 明确 Phase 1 先用模拟数据打通「上报 → 校验 → 幂等 → 落库」，不接真实工具。
- **涉及文件**：platform/docs/schema.md、platform/README.md、platform/shared/schema/event.schema.json、规划/总则/执行计划.md、开发日志.md。
- **关联决策**：决策记录 #26。

## 2026-06-03 — 部分冻结：数据契约 + 接入契约转移 SSOT 至 platform/（决策 #26 取 (a)）

- **阶段**：Phase 0 收尾。按用户确认对 `规划/` 执行**部分冻结**（非全量）。
- **做了什么**：
  - 给 [规划/共享层/数据契约](规划/共享层/数据契约.md)、[规划/数据端/接入契约](规划/数据端/接入契约.md) 顶部加 🧊 冻结横幅，指明 SSOT 已转至 `platform/docs/schema.md`+`event.schema.json` / `platform/docs/contract.md`，原件不再改。
  - CLAUDE.md：§1 SSOT 生命周期改写为「逐份转移 / 部分冻结」（列清已冻结两份 vs 仍活在 `规划/` 的 5 份，避免悬空指针）；§2 这两行的「先读/可能要改」指向改到 `platform/`；§3 数据契约字段连带项改为「改 platform/docs/schema.md + event.schema.json + 建表 SQL + 重跑代码生成」。
  - 其余 5 份（架构与原则 / 执行计划 / 代码规范 / 工具注册表 / 工具门户）**不动**，仍以 `规划/` 为 SSOT。
  - PROJECT_PLAN：决策 #26 由「待定」改为结论 (a)；下一步更新为进 Phase 1。
- **涉及文件**：规划/共享层/数据契约.md、规划/数据端/接入契约.md、CLAUDE.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #26（结论）。

## 2026-06-03 — 产出 Phase 0 实质内容（schema / 建表 SQL / 落地文档 / 机器闸门骨架）；先 git init 建基线

- **阶段**：执行 [执行计划](规划/总则/执行计划.md) §三 交接命令的「严格 Phase 0」（用户确认只做 Phase 0，不实现业务逻辑）。
- **做了什么**：
  - **git init + 基线提交**：本目录原非 Git 仓库，按 CLAUDE.md「规范须纳入版本管理」先初始化、加 `.gitignore`，提交规划文档 + 现有骨架作追溯基线。
  - **Phase 0 实质产出**（落 `platform/`）：
    - `shared/schema/event.schema.json` —— 数据契约 v0.2 的可校验 JSON Schema（圈一 8 项必填 + 圈二/圈三选填，`status`/`cost_source` 枚举，`metadata` 自由口袋）。**唯一源**，各层模型由它生成。
    - `docs/schema.md`、`docs/contract.md` —— 字段落地文档（含 token 归一化、敏感策略「待人工确认」占位）与给工具方的接入契约说明。
    - `backend/storage/migrations/0001_init.sql` —— 三表 DDL：事件表（JSONB metadata，`tool_id`/`conversation_id`/`start_time`/`status` 索引）、工具注册表（接入 + 门户展示字段）、用户账号表（仅存哈希）。价格表按决策 #6 不建，仅注释留位。
    - `README.md`（项目概述：三原则/五层/两前端+共享后端/三类工具）、`docker-compose.yml` 骨架（Postgres + 接入 API/两前端占位）、`.env.example`。
  - **机器闸门骨架**（强度放原型档，宽松能跑）：`platform/pyproject.toml`（ruff/mypy/vulture/import-linter/pytest）、两前端 `package.json`+`eslint.config.js`+`tsconfig.json`+`knip.json`、共享 `.prettierrc.json`、根 `.pre-commit-config.yaml` + `.github/workflows/ci.yml`（重关卡先 continue-on-error，合主干前转必过）、`scripts/gen-models.{sh,ps1}`（schema→Pydantic/TS 代码生成，生成物标「自动生成，勿手改」）。
  - **未做**（守交接命令【不要做】）：未实现 SDK/转发/分析层/前端业务逻辑；未接真实工具/中转站；零硬编码密钥地址；未替用户定敏感内容策略（留占位）。
  - **冻结未执行**：按 CLAUDE.md 本应触发 `规划/` 冻结 + SSOT 转移，但本轮只产出了 schema/接入契约的 platform 对应物，架构与原则/执行计划/代码规范/工具注册表/工具门户尚无 platform/ 等价文档——全量转移会产生悬空指针。**冻结方式留待用户确认**（见决策 #26）。
- **涉及文件**：新增 `.gitignore`、`.pre-commit-config.yaml`、`.github/workflows/ci.yml`、`platform/` 下 schema/docs/SQL/README/compose/pyproject/scripts/两前端配置共十余个；改 `platform/README.md`、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #26（呼应 #1–#6、#13、#14）。

## 2026-06-03 — 门户 UI 纳入正式方案：视觉参考稿由「探讨稿」升为「已确认」

- **阶段**：仍只做规划。状态变更，未触代码。
- **做了什么**：
  - 按用户「纳入正式方案」指示，把门户首页参考稿从「非 SSOT，仅视觉探讨」升级为「**已确认的 UI 视觉参考稿**」：改文件头与标题状态标签；从正式规范 [工具门户](规划/使用端/工具门户.md) §三 顶部正式引用它，并申明「规范 SSOT 仍是 §三，参考稿不另立 SSOT」（守 CLAUDE.md 单一信息源）。
  - 文件位置不动（避免改断既有链接，最小改动）；「mockups」仅为目录名，不影响其正式状态。
  - PROJECT_PLAN 决策 #25 追加「已纳入正式方案、以参考稿为视觉基准」。
- **涉及文件**：规划/使用端/mockups/门户首页-mockup.html、规划/使用端/工具门户.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #25（确认）。

## 2026-06-03 — 门户 UI mockup 落地；定「浅/深双主题日夜切换」、「我的常用」改名「我的收藏」

- **阶段**：仍只做规划。本轮产出可点 mockup + 据反馈微调 UI 决策，未触代码。
- **做了什么**：
  - 新增高保真 mockup [规划/使用端/mockups/门户首页-mockup.html](规划/使用端/mockups/门户首页-mockup.html)：单文件、可切日/夜、可搜索过滤、可点 ✨ 展开 AI 推荐面板、☆ 收藏切换；含有缩略图卡与缺图占位卡两种演示。**非 SSOT，仅视觉探讨**。
  - 据用户反馈定两处设计变更：① 不再分多套方案，**改为同一套布局 + 顶栏（吸顶）日 / 夜主题切换、默认浅色**（原 A/C 两套合一）；② 个性化区「**我的常用**」**改名「我的收藏**」（与 ☆ 收藏语义一致）。
  - 同步规划：[使用端/工具门户](规划/使用端/工具门户.md) §三（顶栏吸顶+日夜切换、双主题、线框/交互/状态里「常用」改「收藏」）；PROJECT_PLAN 决策 #25 补双主题、改「我的收藏」。
- **涉及文件**：规划/使用端/mockups/门户首页-mockup.html（新增）、规划/使用端/工具门户.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #25（修订）。

## 2026-06-03 — 使用端门户 UI 定稿（布局 / 卡片 / 交互），注册表加 thumbnail

- **阶段**：仍只做规划。本轮是使用端前端的界面设计，落进规划文档，未触代码。
- **做了什么**：
  - 与用户逐项敲定门户 UI：布局＝居中搜索框 + 向下分区（启动器风、桌面优先、浅色）；首屏先「我的常用 / 最近使用」再分类分区；卡片＝**图片式媒体卡**（功能缩略图占主体 + icon + 名称 + 简介 + 收藏星标）；搜索即时本地过滤、AI 推荐由显式「✨ 推荐工具」按钮按需触发（省 token）；★ 收藏入常用区、可拖拽排序；门户必须登录。
  - [使用端/工具门户](规划/使用端/工具门户.md) 新增「§三 界面设计（UI）」（含首页线框、卡片解剖、三块交互、状态边角），原§三/§四顺延为§四/§五，§二.1 卡片描述改为缩略图为主体。
  - [共享层/工具注册表](规划/共享层/工具注册表.md) 展示字段新增 `thumbnail`（功能缩略图，选填，缺图自动生成占位图），并细化 `icon` 用途。
  - 同步：PROJECT_PLAN 决策 #23 补 `thumbnail`、新增决策 #25（UI 定稿）。
- **涉及文件**：规划/使用端/工具门户.md、规划/共享层/工具注册表.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #25（呼应 #23、#24）。

## 2026-06-03 — 平台拆为「两个前端 + 共享后端」，文档与代码区按使用端/数据端/共享层重组

- **阶段**：仍只做规划与脚手架。本轮只建代码骨架 + README 占位，**未生成 Phase 0 实质产出，`规划/` 未冻结、仍是 SSOT**。
- **做了什么**：
  - 明确平台为**两个前端**（使用端·使用者门户 / 数据端·后台分析台）**+ 共享后端**，新增使用端门户三大功能（分类卡片、排序、AI 工具推荐）；后台「AI 数据分析」助手留位后置。
  - **重组 `规划/`** 为四组：`总则/`（架构与原则←01、执行计划←04、代码规范←05）、`共享层/`（数据契约←02、工具注册表←从 03 抽出并升级）、`数据端/`（接入契约←03 去注册表）、`使用端/`（工具门户，新建）。旧 `规划/01–05` 已删除（非注释保留）。
  - **架构升级**：架构与原则的五层图把展示层拆成两个并列前端、存储层标共享三表；执行计划 Phase 4 拆两前端、目录结构改为 `platform/`（两个前端+共享后端+集成层）、交接命令改指向 `platform/` 与新文档路径。
  - **工具注册表升级为共享资产**：增 `category`/`display_name`/`description`/`icon`/`launch_url`/`sort_weight`/`enabled` 展示字段。
  - **建代码区 `platform/` 骨架**：`shared`(schema/contracts/registry)、`backend`(ingestion/storage/analytics/auth)、`collection`(sdk/relay)、`integrations`(_template，存放各工具接入适配)、`apps`(user-portal/admin-dashboard)、`docs`，每个目录配 README 占位（只链接回 `规划/`，不复制规范）。
  - **同步索引/守则**：CLAUDE.md §0、§1 结构、SSOT 生命周期、§2/§3 表、§5 链接；PROJECT_PLAN 文档地图整表重写 + 决策 #22–#24；修正全部跨文档相对链接（子目录间 `../<组>/<文件>`）。
- **涉及文件**：删 规划/01–05；新增 规划/{总则,共享层,数据端,使用端}/* 七份；新增 platform/ 全套骨架与 README；改 CLAUDE.md、PROJECT_PLAN.md、开发日志.md。
- **关联决策**：决策记录 #22–#24（呼应 #9、#16）。

## 2026-06-03 — 平台定位升级为入口门户，user_id / 内容字段 / 采集主路随之调整

- **阶段**：数据接入阶段（规划设计），仍只做规划与脚手架。
- **做了什么**：
  - 将平台从「被动汇总站」升级为「统一登录入口门户」：使用者从入口打开工具、平台注入 `user_id`，绕过入口记匿名；新增用户账号体系（一人一号、发放、自改、存哈希）。
  - `user_id`/`team_id` 改为「门户注入，否则匿名」；`input_content`/`output_content` 从 LLM 专属移为通用选填——**契约升 v0.2**。
  - 明确采集主路为「工具自报」（参考 SDK 为其便捷封装），转发 / 独立 key 仅黑盒兜底、非必经总闸。
  - 明确「一次使用」由工具按触发点自定义（触发式埋点），不调大模型的工具可只记圈一 + 可选内容。
  - 同步：01 新增「入口门户与账号体系」节、改三类工具表、统一 §三架构图（采集层标自报、展示层加登录/账号、边框按显示宽度重排）；02 升版本、迁移内容字段、改 user_id 语义、补 conversation_id 说明；03 自报主路与门户身份义务、注册表 collect_method、边角情况；04 Phase/目录/交接命令补用户账号表与登录入口；PROJECT_PLAN 决策 #16–#21 + 文档地图；CLAUDE.md §1 结构描述。
- **涉及文件**：规划/01、02、03、04，PROJECT_PLAN.md，开发日志.md，CLAUDE.md。
- **关联决策**：决策记录 #16–#21。

## 2026-06-02 — 项目规划成型：数据契约定稿、文档体系拆分、工程规范立项

- **阶段**：数据接入阶段（规划设计），只做规划与脚手架，不实现业务逻辑。
- **做了什么**：
  - 审阅初版计划，把讨论谈定的取舍系统性补入，形成决策记录共 14 条。
  - 对照 APIMart 接口文档敲定数据契约细节：token 由采集端归一化、缓存/推理 token 暂入 metadata、`cost` 区分来源、流式须开 usage、异步任务两段式；新增 `schema_version`、`conversation_id`、`ingested_at`、`cost_source` 等字段。
  - 把原先单一的 PROJECT_PLAN 按「读者 + 变更频率」拆成五份规范（架构与原则 / 数据契约 / 接入契约 / 执行计划 / 代码规范），PROJECT_PLAN 瘦身为总纲（含文档地图 + 决策记录）。
  - 新建 CLAUDE.md 文档维护守则：单一信息源、按任务读哪份、改动传播规则、长期维护原则（重构不打补丁、删不注释）、改完自检清单、SSOT 部署后转移规则。
  - 锁定技术栈 Python + FastAPI + React/TS；制定代码规范以机器闸门（格式化/lint/类型/死代码扫描/分层依赖/测试）治理「越写越乱、AI 残留」，并明确规范为可改默认、闸门按阶段校准。
  - 更新交接命令：脚手架部署进子目录，附带机器闸门与代码生成骨架。
  - 新建本开发日志，并在 CLAUDE.md 立规：今后任何改动都要追加一条日志。
- **涉及文件**：PROJECT_PLAN.md（瘦身为总纲 + 决策记录 #1–#15 + 文档地图）、CLAUDE.md（新增，含日志规则）、规划/01–05（新增五份）、开发日志.md（新增）。
- **关联决策**：决策记录 #1–#15。
