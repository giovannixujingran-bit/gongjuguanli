# 钉钉免登打通数据门禁 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 钉钉客户端内打开门户即免登拿身份，统计+明细仅 admin/超管可见，超管可在面板增减 admin，密码登录退役。

**Architecture:** 前台用钉钉 JSAPI 拿免登 code → 后端 `/auth/dingtalk` 用复用的 `HttpxDingtalkClient` 把 code 换成 dingtalk_userid → 按 userid 查 `user_account`（组织同步已建）→ 判定身份（超管由配置 `BOOTSTRAP_ADMIN_DINGTALK_USERID` 认定、admin 由 DB `role` 认定）→ 签发带 `is_superadmin` 的 token。数据端读接口收紧为「admin 或超管」，admin 管理接口收紧为「仅超管」。

**Tech Stack:** Python 3.12 / FastAPI / psycopg / httpx（后端）；原生 JS + 钉钉 JSAPI（`dd.runtime.permission.requestAuthCode`）（前台）。测试用 fastapi `TestClient` + 内存仓库，不打真实钉钉云、不需要 DB。

**设计依据：** [2026-06-12-钉钉免登数据门禁-design.md](2026-06-12-钉钉免登数据门禁-design.md)

**运行测试（本机离线环境）：** 见 [local-test-env]，统一用 `platform/` 下的 `.pydeps` + codex-runtime Python 3.12：
```
cd platform; pytest backend/auth backend/org_sync backend/ingestion -q
```

---

## 角色判定（贯穿全计划的统一口径）

- `is_superadmin(userid)` = `userid ∈ BOOTSTRAP_ADMIN_DINGTALK_USERID`（逗号分隔配置）。
- `can_view_data`（看统计+明细+AI 查询）= `claims.role == 'admin'` 或 `claims.is_superadmin`。
- `can_manage_admins`（增减 admin）= `claims.is_superadmin`。
- 超管隐含拥有 admin 的一切（即使 DB role 仍是 user，登录时按 admin 待遇签发 `role='admin'` 且 `is_superadmin=True`）。

---

## Task 1: token claims 增加 is_superadmin

**Files:**
- Modify: `platform/backend/auth/tokens.py`
- Test: `platform/backend/auth/test_tokens.py`

- [ ] **Step 1: 写失败测试**

在 `test_tokens.py` 末尾追加：

```python
def test_issue_and_verify_token_carries_is_superadmin() -> None:
    account = UserAccount(
        user_id="user-001",
        username="alice",
        password_hash=None,
        team_id=None,
        role="admin",
    )

    token, _ = issue_token(account, secret="secret", is_superadmin=True)
    claims = verify_token(token, secret="secret")

    assert claims is not None
    assert claims.is_superadmin is True


def test_token_defaults_is_superadmin_false() -> None:
    account = UserAccount(
        user_id="user-002",
        username="bob",
        password_hash=None,
        team_id=None,
        role="user",
    )
    token, _ = issue_token(account, secret="secret")
    claims = verify_token(token, secret="secret")
    assert claims is not None
    assert claims.is_superadmin is False
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/auth/test_tokens.py -q`
Expected: FAIL（`issue_token() got an unexpected keyword argument 'is_superadmin'`）

- [ ] **Step 3: 最小实现**

在 `tokens.py`：`TokenClaims` 增加字段（放在 `team_id` 后、`expires_at` 前）：

```python
@dataclass(frozen=True)
class TokenClaims:
    user_id: str
    username: str
    role: str
    team_id: str | None
    is_superadmin: bool
    expires_at: int
```

`issue_token` 增加参数并写入 claims：

```python
def issue_token(
    account: UserAccount,
    *,
    secret: str,
    is_superadmin: bool = False,
    ttl_seconds: int = DEFAULT_TOKEN_TTL_SECONDS,
) -> tuple[str, int]:
    expires_at = int(time.time()) + ttl_seconds
    claims = TokenClaims(
        user_id=account.user_id,
        username=account.username,
        role=account.role,
        team_id=account.team_id,
        is_superadmin=is_superadmin,
        expires_at=expires_at,
    )
    payload = encode_json(claims.__dict__)
    signature = sign(payload, secret)
    return f"{payload}.{signature}", expires_at
```

`verify_token` 解析新字段（旧 token 无此字段时默认 False，向后兼容）：

```python
    return TokenClaims(
        user_id=str(data["user_id"]),
        username=str(data["username"]),
        role=str(data["role"]),
        team_id=None if data.get("team_id") is None else str(data["team_id"]),
        is_superadmin=bool(data.get("is_superadmin", False)),
        expires_at=int(data["expires_at"]),
    )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/auth/test_tokens.py -q`
Expected: PASS（原有两个测试 + 新增两个）

- [ ] **Step 5: 提交**

```bash
git add platform/backend/auth/tokens.py platform/backend/auth/test_tokens.py
git commit -m "feat(auth): token claims 增加 is_superadmin"
```

---

## Task 2: 账号仓库增加 dingtalk_userid 查询、列表、改角色

**Files:**
- Modify: `platform/backend/auth/accounts.py`
- Test: `platform/backend/auth/test_accounts_repo.py`（新建，纯协议契约用内存假实现测；Postgres 实现走库不在单测）

说明：`UserAccount` 不加 `dingtalk_userid`（免登流程从钉钉换得 userid，不依赖账号对象带它）。新增一个只读汇总类 `AccountSummary` 给管理面板用。

- [ ] **Step 1: 写失败测试**

新建 `platform/backend/auth/test_accounts_repo.py`：

```python
from __future__ import annotations

from backend.auth.accounts import AccountSummary


def test_account_summary_shape() -> None:
    summary = AccountSummary(
        user_id="u1",
        dingtalk_userid="dd-1",
        display_name="张三",
        role="admin",
    )
    assert summary.user_id == "u1"
    assert summary.dingtalk_userid == "dd-1"
    assert summary.display_name == "张三"
    assert summary.role == "admin"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/auth/test_accounts_repo.py -q`
Expected: FAIL（`cannot import name 'AccountSummary'`）

- [ ] **Step 3: 最小实现**

在 `accounts.py` 增加数据类与协议方法、Postgres 实现：

```python
@dataclass(frozen=True)
class AccountSummary:
    user_id: str
    dingtalk_userid: str | None
    display_name: str | None
    role: Role
```

`UserAccountRepository` Protocol 增加三个方法签名：

```python
    def get_by_dingtalk_userid(self, dingtalk_userid: str) -> UserAccount | None:
        """Find one account by its synced DingTalk userid."""

    def list_accounts(self) -> list[AccountSummary]:
        """List all accounts for the admin-management panel."""

    def set_role(self, user_id: str, role: Role) -> None:
        """Set an account's role (admin management)."""
```

`PostgresUserAccountRepository` 增加实现：

```python
    def get_by_dingtalk_userid(self, dingtalk_userid: str) -> UserAccount | None:
        return self._fetch_one("dingtalk_userid", dingtalk_userid)

    def list_accounts(self) -> list[AccountSummary]:
        with db_connect(self._database_url) as connection:
            with connection.cursor(row_factory=dict_row) as cursor:
                cursor.execute(
                    """
                    SELECT user_id, dingtalk_userid, display_name, role
                    FROM user_account
                    ORDER BY display_name NULLS LAST, username
                    """
                )
                rows = cursor.fetchall()
        return [
            AccountSummary(
                user_id=str(r["user_id"]),
                dingtalk_userid=None if r["dingtalk_userid"] is None else str(r["dingtalk_userid"]),
                display_name=None if r["display_name"] is None else str(r["display_name"]),
                role="admin" if r["role"] == "admin" else "user",
            )
            for r in rows
        ]

    def set_role(self, user_id: str, role: Role) -> None:
        with db_connect(self._database_url) as connection:
            with connection.cursor() as cursor:
                cursor.execute(
                    "UPDATE user_account SET role = %(role)s, updated_at = now() "
                    "WHERE user_id = %(user_id)s",
                    {"role": role, "user_id": user_id},
                )
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/auth/test_accounts_repo.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/auth/accounts.py platform/backend/auth/test_accounts_repo.py
git commit -m "feat(auth): 账号仓库增加 dingtalk_userid 查询/列表/改角色"
```

---

## Task 3: 钉钉客户端增加免登 code 换 userid

**Files:**
- Modify: `platform/backend/org_sync/client.py`
- Test: `platform/backend/org_sync/test_client_userinfo.py`（新建，用 `httpx.MockTransport` 模拟钉钉响应）

- [ ] **Step 1: 写失败测试**

新建 `platform/backend/org_sync/test_client_userinfo.py`：

```python
from __future__ import annotations

import httpx

from backend.org_sync.client import HttpxDingtalkClient


def _mock_transport() -> httpx.MockTransport:
    def handler(request: httpx.Request) -> httpx.Response:
        if request.url.path == "/gettoken":
            return httpx.Response(200, json={"errcode": 0, "access_token": "tok", "expires_in": 7200})
        if request.url.path == "/user/getuserinfo":
            assert request.url.params.get("access_token") == "tok"
            assert request.url.params.get("code") == "免登码123"
            return httpx.Response(200, json={"errcode": 0, "userid": "dd-userid-9"})
        return httpx.Response(404, json={"errcode": 60020, "errmsg": "not found"})

    return httpx.MockTransport(handler)


def test_get_userinfo_by_code_returns_userid() -> None:
    http = httpx.Client(transport=_mock_transport(), base_url="https://oapi.dingtalk.com")
    client = HttpxDingtalkClient(client_id="k", client_secret="s", http=http)

    assert client.get_userinfo_by_code("免登码123") == "dd-userid-9"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/org_sync/test_client_userinfo.py -q`
Expected: FAIL（`'HttpxDingtalkClient' object has no attribute 'get_userinfo_by_code'`）

- [ ] **Step 3: 最小实现**

在 `client.py` 的 `DingtalkClient` Protocol 增加方法签名：

```python
    def get_userinfo_by_code(self, code: str) -> str:
        """用免登 code 换 dingtalk userid。"""
```

在 `HttpxDingtalkClient` 增加实现（GET 经典免登接口，复用 `_access_token` 缓存）：

```python
    def get_userinfo_by_code(self, code: str) -> str:
        endpoint = "/user/getuserinfo"
        response = self._http.get(
            f"{self._base_url}{endpoint}",
            params={"access_token": self._access_token(), "code": code},
        )
        response.raise_for_status()
        payload = _require_ok(endpoint, _require_dict(endpoint, response.json()))
        userid = payload.get("userid")
        if not isinstance(userid, str) or not userid:
            raise DingtalkApiError(endpoint, -1, "missing userid")
        return userid
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/org_sync/test_client_userinfo.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/org_sync/client.py platform/backend/org_sync/test_client_userinfo.py
git commit -m "feat(org_sync): 钉钉客户端增加免登 code 换 userid"
```

---

## Task 4: 超管配置解析

**Files:**
- Create: `platform/backend/auth/superadmin.py`
- Test: `platform/backend/auth/test_superadmin.py`

- [ ] **Step 1: 写失败测试**

新建 `platform/backend/auth/test_superadmin.py`：

```python
from __future__ import annotations

from backend.auth.superadmin import parse_superadmin_userids


def test_parse_comma_separated() -> None:
    s = parse_superadmin_userids("dd-1, dd-2 ,, dd-3")
    assert s == {"dd-1", "dd-2", "dd-3"}


def test_parse_none_or_blank_is_empty() -> None:
    assert parse_superadmin_userids(None) == set()
    assert parse_superadmin_userids("   ") == set()
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/auth/test_superadmin.py -q`
Expected: FAIL（`No module named 'backend.auth.superadmin'`）

- [ ] **Step 3: 最小实现**

新建 `platform/backend/auth/superadmin.py`：

```python
from __future__ import annotations


def parse_superadmin_userids(raw: str | None) -> set[str]:
    """把逗号分隔的钉钉 userid 配置解析成集合，忽略空白项。"""
    if not raw:
        return set()
    return {part.strip() for part in raw.split(",") if part.strip()}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/auth/test_superadmin.py -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/auth/superadmin.py platform/backend/auth/test_superadmin.py
git commit -m "feat(auth): 超管 userid 配置解析"
```

---

## Task 5: 内存账号仓库补齐新方法（为后续 app 测试铺路）

**Files:**
- Modify: `platform/backend/ingestion/test_app.py`（`MemoryUserAccountRepository`）

- [ ] **Step 1: 扩展内存仓库**

在 `test_app.py` 的 `MemoryUserAccountRepository`：
1. import 增加 `AccountSummary`：把第 10 行的 import 改为
   `from backend.auth.accounts import AccountSummary, Role, UserAccount, UserAccountRepository`
2. `create_user` 增加可选 `dingtalk_userid` 入参并存储；新增按 dingtalk_userid 的索引与三个新方法：

```python
@dataclass
class MemoryUserAccountRepository:
    users_by_username: dict[str, UserAccount] = field(default_factory=dict)
    users_by_id: dict[str, UserAccount] = field(default_factory=dict)
    users_by_dingtalk: dict[str, UserAccount] = field(default_factory=dict)
    display_names: dict[str, str] = field(default_factory=dict)
    dingtalk_ids: dict[str, str] = field(default_factory=dict)

    def create_user(
        self,
        *,
        username: str,
        password: str,
        user_id: str | None = None,
        team_id: str | None = None,
        role: Role = "user",
        dingtalk_userid: str | None = None,
        display_name: str | None = None,
    ) -> UserAccount:
        account = UserAccount(
            user_id=user_id or f"user-{len(self.users_by_id) + 1}",
            username=username,
            password_hash=hash_password(password) if password else None,
            team_id=team_id,
            role=role,
        )
        self.users_by_username[username] = account
        self.users_by_id[account.user_id] = account
        if dingtalk_userid is not None:
            self.users_by_dingtalk[dingtalk_userid] = account
            self.dingtalk_ids[account.user_id] = dingtalk_userid
        if display_name is not None:
            self.display_names[account.user_id] = display_name
        return account

    def get_by_username(self, username: str) -> UserAccount | None:
        return self.users_by_username.get(username)

    def get_by_user_id(self, user_id: str) -> UserAccount | None:
        return self.users_by_id.get(user_id)

    def get_by_dingtalk_userid(self, dingtalk_userid: str) -> UserAccount | None:
        return self.users_by_dingtalk.get(dingtalk_userid)

    def list_accounts(self) -> list[AccountSummary]:
        return [
            AccountSummary(
                user_id=a.user_id,
                dingtalk_userid=self.dingtalk_ids.get(a.user_id),
                display_name=self.display_names.get(a.user_id),
                role=a.role,
            )
            for a in self.users_by_id.values()
        ]

    def set_role(self, user_id: str, role: Role) -> None:
        old = self.users_by_id[user_id]
        updated = UserAccount(
            user_id=old.user_id,
            username=old.username,
            password_hash=old.password_hash,
            team_id=old.team_id,
            role=role,
        )
        self.users_by_id[user_id] = updated
        self.users_by_username[old.username] = updated
        dd = self.dingtalk_ids.get(user_id)
        if dd is not None:
            self.users_by_dingtalk[dd] = updated
```

- [ ] **Step 2: 跑现有测试确认未破坏**

Run: `cd platform; pytest backend/ingestion/test_app.py -q`
Expected: PASS（此步只加方法，不改行为）

- [ ] **Step 3: 提交**

```bash
git add platform/backend/ingestion/test_app.py
git commit -m "test(app): 内存账号仓库补齐 dingtalk/列表/改角色方法"
```

---

## Task 6: `/auth/dingtalk` 免登换 token + 测试 seam

**Files:**
- Modify: `platform/backend/ingestion/app.py`
- Test: `platform/backend/ingestion/test_app.py`

引入一个钉钉免登客户端依赖 seam：`DingtalkAuthClient` Protocol（只有 `get_userinfo_by_code`），生产实现用 `HttpxDingtalkClient`，测试用假实现覆盖。

- [ ] **Step 1: 写失败测试**

在 `test_app.py` 增加假免登客户端 + 构造带免登的 client 的 helper（放在 `client_with_repositories` 附近）：

```python
@dataclass
class FakeDingtalkAuthClient:
    code_to_userid: dict[str, str] = field(default_factory=dict)

    def get_userinfo_by_code(self, code: str) -> str:
        if code not in self.code_to_userid:
            raise KeyError(code)
        return self.code_to_userid[code]
```

并在文件顶部 import 增加：
`from backend.ingestion.app import get_dingtalk_auth_client, get_superadmin_userids`

新增测试：

```python
def test_dingtalk_login_issues_token_for_synced_user() -> None:
    user_repository = MemoryUserAccountRepository()
    user_repository.create_user(
        username="dd-100", password="", user_id="user-100",
        role="user", dingtalk_userid="dd-100", display_name="李四",
    )
    app = create_app()
    app.dependency_overrides[get_user_repository] = lambda: user_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_dingtalk_auth_client] = (
        lambda: FakeDingtalkAuthClient({"code-1": "dd-100"})
    )
    app.dependency_overrides[get_superadmin_userids] = lambda: set()
    client = TestClient(app)

    resp = client.post("/auth/dingtalk", json={"code": "code-1"})

    assert resp.status_code == 200
    body = resp.json()
    assert body["user"]["user_id"] == "user-100"
    assert body["user"]["role"] == "user"


def test_dingtalk_login_unsynced_user_is_rejected() -> None:
    user_repository = MemoryUserAccountRepository()
    app = create_app()
    app.dependency_overrides[get_user_repository] = lambda: user_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_dingtalk_auth_client] = (
        lambda: FakeDingtalkAuthClient({"code-x": "dd-unknown"})
    )
    app.dependency_overrides[get_superadmin_userids] = lambda: set()
    client = TestClient(app)

    resp = client.post("/auth/dingtalk", json={"code": "code-x"})

    assert resp.status_code == 403
    assert "未同步" in resp.json()["detail"]


def test_dingtalk_login_superadmin_gets_admin_treatment() -> None:
    user_repository = MemoryUserAccountRepository()
    user_repository.create_user(
        username="dd-boss", password="", user_id="user-boss",
        role="user", dingtalk_userid="dd-boss", display_name="老板",
    )
    app = create_app()
    app.dependency_overrides[get_user_repository] = lambda: user_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_dingtalk_auth_client] = (
        lambda: FakeDingtalkAuthClient({"c": "dd-boss"})
    )
    app.dependency_overrides[get_superadmin_userids] = lambda: {"dd-boss"}
    client = TestClient(app)

    resp = client.post("/auth/dingtalk", json={"code": "c"})

    assert resp.status_code == 200
    assert resp.json()["user"]["role"] == "admin"
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/ingestion/test_app.py -k dingtalk_login -q`
Expected: FAIL（`cannot import name 'get_dingtalk_auth_client'`）

- [ ] **Step 3: 最小实现**

在 `app.py`：

import 增加：
```python
from backend.auth.accounts import AccountSummary  # 加入现有 accounts import 块
from backend.auth.superadmin import parse_superadmin_userids
from backend.auth.tokens import TokenClaims, bearer_token, issue_token, verify_token  # issue_token 已在
from backend.org_sync.client import HttpxDingtalkClient
from typing import Protocol  # 若未导入
```

新增请求模型 + Protocol + 依赖：

```python
class DingtalkLoginRequest(BaseModel):
    code: str = Field(min_length=1)


class DingtalkAuthClient(Protocol):
    def get_userinfo_by_code(self, code: str) -> str: ...


@lru_cache(maxsize=1)
def get_superadmin_userids() -> set[str]:
    return parse_superadmin_userids(os.environ.get("BOOTSTRAP_ADMIN_DINGTALK_USERID"))


@lru_cache(maxsize=1)
def get_dingtalk_auth_client() -> DingtalkAuthClient:
    client_id = os.environ.get("DINGTALK_CLIENT_ID")
    client_secret = os.environ.get("DINGTALK_CLIENT_SECRET")
    if not client_id or not client_secret:
        raise RuntimeError("DINGTALK_CLIENT_ID / DINGTALK_CLIENT_SECRET are required for免登")
    return HttpxDingtalkClient(client_id=client_id, client_secret=client_secret)
```

新增端点（放在 `/auth/login` 附近）：

```python
    @application.post("/auth/dingtalk", response_model=TokenResponse)
    def dingtalk_login(
        request: DingtalkLoginRequest,
        repository: UserAccountRepository = Depends(get_user_repository),
        auth_client: DingtalkAuthClient = Depends(get_dingtalk_auth_client),
        secret: str = Depends(get_auth_token_secret),
        superadmins: set[str] = Depends(get_superadmin_userids),
    ) -> TokenResponse:
        try:
            dingtalk_userid = auth_client.get_userinfo_by_code(request.code)
        except Exception as exc:  # 免登 code 无效/过期/钉钉不可达
            raise HTTPException(
                status_code=status.HTTP_401_UNAUTHORIZED,
                detail="钉钉免登失败，请重试或重新打开页面",
            ) from exc
        account = repository.get_by_dingtalk_userid(dingtalk_userid)
        if account is None:
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="账号未同步，请联系超管在后台同步组织",
            )
        is_superadmin = dingtalk_userid in superadmins
        if is_superadmin and account.role != "admin":
            # 超管隐含 admin 待遇：签发的 token 直接给 admin 角色
            account = UserAccount(
                user_id=account.user_id,
                username=account.username,
                password_hash=account.password_hash,
                team_id=account.team_id,
                role="admin",
            )
        token, expires_at = issue_token(account, secret=secret, is_superadmin=is_superadmin)
        return TokenResponse(access_token=token, expires_at=expires_at, user=public_user(account))
```

`public_user` / `public_claims` 已含 role，无需改。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/ingestion/test_app.py -k dingtalk_login -q`
Expected: PASS（3 个）

- [ ] **Step 5: 提交**

```bash
git add platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "feat(auth): /auth/dingtalk 免登换 token（含超管隐含 admin）"
```

---

## Task 7: 数据端门禁 require_data_viewer + 超管门禁 require_superadmin

**Files:**
- Modify: `platform/backend/ingestion/app.py`
- Test: `platform/backend/ingestion/test_app.py`

- [ ] **Step 1: 写失败测试**

在 `test_app.py` 增加一个用免登铸 token 的 helper 和门禁测试：

```python
def login_via_dingtalk(client, code: str) -> str:
    return client.post("/auth/dingtalk", json={"code": code}).json()["access_token"]


def viewer_client(role: str, *, superadmin: bool):
    user_repository = MemoryUserAccountRepository()
    user_repository.create_user(
        username="dd-v", password="", user_id="user-v",
        role=role, dingtalk_userid="dd-v", display_name="测试",
    )
    app = create_app()
    app.dependency_overrides[get_user_repository] = lambda: user_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_event_reader] = lambda: MemoryUsageEventReader(
        page=EventPage(rows=[sample_event_row()], total=1), summary=SAMPLE_SUMMARY
    )
    app.dependency_overrides[get_tool_directory] = lambda: MemoryToolDirectory()
    app.dependency_overrides[get_dingtalk_auth_client] = (
        lambda: FakeDingtalkAuthClient({"c": "dd-v"})
    )
    app.dependency_overrides[get_superadmin_userids] = (
        lambda: {"dd-v"} if superadmin else set()
    )
    return TestClient(app)


def test_summary_requires_data_viewer() -> None:
    client = viewer_client("user", superadmin=False)
    token = login_via_dingtalk(client, "c")
    resp = client.get("/analytics/summary", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 403


def test_summary_allows_admin() -> None:
    client = viewer_client("admin", superadmin=False)
    token = login_via_dingtalk(client, "c")
    resp = client.get("/analytics/summary", headers={"Authorization": f"Bearer {token}"})
    assert resp.status_code == 200


def test_events_and_aiquery_require_data_viewer() -> None:
    client = viewer_client("user", superadmin=False)
    token = login_via_dingtalk(client, "c")
    h = {"Authorization": f"Bearer {token}"}
    assert client.get("/analytics/events", headers=h).status_code == 403
    assert client.post("/ai/query", json={"question": "x"}, headers=h).status_code == 403
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/ingestion/test_app.py -k "data_viewer or aiquery" -q`
Expected: FAIL（当前 `/analytics/*` 只要有 token 即 200，不会 403）

- [ ] **Step 3: 最小实现**

在 `app.py` 增加门禁依赖（放在 `require_admin` 附近）：

```python
def require_data_viewer(claims: TokenClaims = Depends(required_token_claims)) -> TokenClaims:
    if claims.role != "admin" and not claims.is_superadmin:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="无权查看数据，请联系超管开通",
        )
    return claims


def require_superadmin(claims: TokenClaims = Depends(required_token_claims)) -> TokenClaims:
    if not claims.is_superadmin:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="仅超管可操作",
        )
    return claims
```

把 `/analytics/events`、`/analytics/summary`、`/ai/query` 三处的
`_viewer: TokenClaims = Depends(required_token_claims)` 改为
`_viewer: TokenClaims = Depends(require_data_viewer)`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/ingestion/test_app.py -k "data_viewer or aiquery" -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "feat(auth): 数据端收紧为 admin/超管，新增 require_superadmin"
```

---

## Task 8: `/auth/admins` 列表 + 改角色（仅超管）

**Files:**
- Modify: `platform/backend/ingestion/app.py`
- Test: `platform/backend/ingestion/test_app.py`

- [ ] **Step 1: 写失败测试**

```python
def admins_client():
    user_repository = MemoryUserAccountRepository()
    user_repository.create_user(
        username="dd-boss", password="", user_id="user-boss",
        role="user", dingtalk_userid="dd-boss", display_name="老板",
    )
    user_repository.create_user(
        username="dd-emp", password="", user_id="user-emp",
        role="user", dingtalk_userid="dd-emp", display_name="员工",
    )
    app = create_app()
    app.dependency_overrides[get_user_repository] = lambda: user_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_dingtalk_auth_client] = (
        lambda: FakeDingtalkAuthClient({"boss": "dd-boss", "emp": "dd-emp"})
    )
    app.dependency_overrides[get_superadmin_userids] = lambda: {"dd-boss"}
    return TestClient(app), user_repository


def test_superadmin_lists_and_promotes_admin() -> None:
    client, repo = admins_client()
    boss = login_via_dingtalk(client, "boss")
    h = {"Authorization": f"Bearer {boss}"}

    listing = client.get("/auth/admins", headers=h)
    assert listing.status_code == 200
    assert any(r["display_name"] == "员工" for r in listing.json())

    promote = client.post("/auth/admins/user-emp", headers=h, json={"role": "admin"})
    assert promote.status_code == 200
    assert repo.get_by_user_id("user-emp").role == "admin"


def test_non_superadmin_cannot_manage_admins() -> None:
    client, _ = admins_client()
    emp = login_via_dingtalk(client, "emp")  # 普通员工
    h = {"Authorization": f"Bearer {emp}"}
    assert client.get("/auth/admins", headers=h).status_code == 403
    assert client.post("/auth/admins/user-boss", headers=h, json={"role": "admin"}).status_code == 403
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/ingestion/test_app.py -k admins -q`
Expected: FAIL（404，端点不存在）

- [ ] **Step 3: 最小实现**

在 `app.py` 增加模型与端点：

```python
class AdminSummaryPublic(BaseModel):
    user_id: str
    dingtalk_userid: str | None = None
    display_name: str | None = None
    role: str


class SetRoleRequest(BaseModel):
    role: Role
```

```python
    @application.get("/auth/admins", response_model=list[AdminSummaryPublic])
    def list_admins(
        repository: UserAccountRepository = Depends(get_user_repository),
        _su: TokenClaims = Depends(require_superadmin),
    ) -> list[AdminSummaryPublic]:
        return [
            AdminSummaryPublic(
                user_id=a.user_id,
                dingtalk_userid=a.dingtalk_userid,
                display_name=a.display_name,
                role=a.role,
            )
            for a in repository.list_accounts()
        ]

    @application.post("/auth/admins/{user_id}", response_model=UserPublic)
    def set_admin_role(
        user_id: str,
        request: SetRoleRequest,
        repository: UserAccountRepository = Depends(get_user_repository),
        _su: TokenClaims = Depends(require_superadmin),
    ) -> UserPublic:
        account = repository.get_by_user_id(user_id)
        if account is None:
            raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="账号不存在")
        repository.set_role(user_id, request.role)
        updated = repository.get_by_user_id(user_id)
        assert updated is not None
        return public_user(updated)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/ingestion/test_app.py -k admins -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "feat(auth): /auth/admins 列表+改角色（仅超管）"
```

---

## Task 9: `/auth/config` 暴露 corpId

**Files:**
- Modify: `platform/backend/ingestion/app.py`
- Test: `platform/backend/ingestion/test_app.py`

- [ ] **Step 1: 写失败测试**

```python
def test_auth_config_exposes_corp_id() -> None:
    app = create_app()
    app.dependency_overrides[get_dingtalk_corp_id] = lambda: "corp-xyz"
    client = TestClient(app)
    resp = client.get("/auth/config")
    assert resp.status_code == 200
    assert resp.json() == {"corp_id": "corp-xyz"}
```

并在顶部 import 增加 `get_dingtalk_corp_id`。

- [ ] **Step 2: 跑测试确认失败**

Run: `cd platform; pytest backend/ingestion/test_app.py -k auth_config -q`
Expected: FAIL（`cannot import name 'get_dingtalk_corp_id'`）

- [ ] **Step 3: 最小实现**

```python
class AuthConfigPublic(BaseModel):
    corp_id: str


@lru_cache(maxsize=1)
def get_dingtalk_corp_id() -> str:
    return os.environ.get("DINGTALK_CORP_ID", "")
```

```python
    @application.get("/auth/config", response_model=AuthConfigPublic)
    def auth_config(corp_id: str = Depends(get_dingtalk_corp_id)) -> AuthConfigPublic:
        return AuthConfigPublic(corp_id=corp_id)
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd platform; pytest backend/ingestion/test_app.py -k auth_config -q`
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "feat(auth): /auth/config 暴露 corpId 供前台免登"
```

---

## Task 10: 删除密码登录，迁移测试 token 铸造

**Files:**
- Modify: `platform/backend/ingestion/app.py`
- Modify: `platform/backend/ingestion/test_app.py`

说明：删除 `/auth/login`、`authenticate` 引用、`LoginRequest`。`/auth/users`、`create_user`、`passwords.py` 保留（`create_user` 仍被组织同步外的内部测试与 `/auth/users` 使用；不扩大删除范围，YAGNI）。所有测试改用 `/auth/dingtalk` 铸 token。

- [ ] **Step 1: 改测试 helper 用免登铸 token**

把 `test_app.py` 里 `client_with_repositories`、`admin_client`、`client_with_repository` 中
「`create_user` + `POST /auth/login`」的 token 铸造，统一替换为：给账号带 `dingtalk_userid`，注册 `get_dingtalk_auth_client` 覆盖，改用 `POST /auth/dingtalk`。

将 `client_with_repositories` 改为（关键部分）：

```python
def client_with_repositories(
    repository,
    *,
    user_repository=None,
    tool_registry_repository=None,
    ai_answerer=None,
    superadmins: set[str] | None = None,
    auth_codes: dict[str, str] | None = None,
):
    app = create_app()
    app.dependency_overrides[get_repository] = lambda: repository
    if user_repository is not None:
        app.dependency_overrides[get_user_repository] = lambda: user_repository
    if tool_registry_repository is not None:
        app.dependency_overrides[get_tool_registry_repository] = lambda: tool_registry_repository
    app.dependency_overrides[get_auth_token_secret] = lambda: "test-secret"
    app.dependency_overrides[get_event_reader] = lambda: MemoryUsageEventReader(
        page=EventPage(rows=[sample_event_row()], total=1), summary=SAMPLE_SUMMARY
    )
    app.dependency_overrides[get_tool_directory] = lambda: MemoryToolDirectory()
    app.dependency_overrides[get_ai_answerer] = lambda: ai_answerer
    app.dependency_overrides[get_dingtalk_auth_client] = lambda: FakeDingtalkAuthClient(
        auth_codes or {}
    )
    app.dependency_overrides[get_superadmin_userids] = lambda: superadmins or set()
    return TestClient(app)
```

把 `admin_client` 改为铸 admin token via 免登（admin 账号带 dingtalk_userid，列入 superadmins 或直接 role=admin）：

```python
def admin_client(*, tool_registry_repository=None):
    user_repository = MemoryUserAccountRepository()
    user_repository.create_user(
        username="dd-root", password="", user_id="admin-001",
        role="admin", dingtalk_userid="dd-root", display_name="管理员",
    )
    client = client_with_repositories(
        MemoryUsageEventRepository(),
        user_repository=user_repository,
        tool_registry_repository=tool_registry_repository,
        auth_codes={"root": "dd-root"},
    )
    admin_token = client.post("/auth/dingtalk", json={"code": "root"}).json()["access_token"]
    return client, admin_token
```

- [ ] **Step 2: 删除/改写依赖 `/auth/login` 的旧测试**

- 删除纯粹验证密码登录的测试：`test_login_*`（按 `grep -n "auth/login" test_app.py` 定位），以及 `test_create_user_then_login_*`、`test_ingest_event_uses_verified_token_identity_over_payload_user_id` 中铸 token 的部分改用 `/auth/dingtalk`（账号带 `dingtalk_userid`，posts code）。
- `test_create_user_requires_admin` / `test_register_tool_requires_admin`：非 admin token 改用免登铸（普通员工账号带 dingtalk_userid、role=user）。

（实现者：逐个把 `client.post("/auth/login", json={"username":X,"password":Y})` 换成
`client.post("/auth/dingtalk", json={"code": CODE})`，并确保对应账号 `create_user(..., dingtalk_userid=DD)` 且 helper 传 `auth_codes={CODE: DD}`。）

- [ ] **Step 3: 删除生产端密码登录**

在 `app.py`：
- 删除 `@application.post("/auth/login", ...)` 整个 `login` 函数。
- 删除 `LoginRequest` 模型。
- 删除 import 里的 `authenticate`（accounts import 块）。

- [ ] **Step 4: 跑全套确认通过**

Run: `cd platform; pytest backend/auth backend/org_sync backend/ingestion -q`
Expected: PASS（无 `/auth/login` 残留引用；`grep -rn "auth/login" platform/backend` 应为空）

- [ ] **Step 5: 提交**

```bash
git add platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "refactor(auth): 删除密码登录，测试改用钉钉免登铸 token（P2 密码退役）"
```

---

## Task 11: 前台接钉钉免登、删密码框、按角色显隐、管理面板

**Files:**
- Modify: `platform/apps/web/index.html`

无自动化测试（静态前台）；以浏览器预览人工核验（见 Task 12 验证）。

- [ ] **Step 1: 引入钉钉 JSAPI 并实现免登 boot**

在 `<head>` 适当位置（其他 script 之前）加钉钉 JSAPI：

```html
<script src="https://g.alicdn.com/dingding/dingtalk-jsapi/3.0.25/dingtalk.open.js"></script>
```

在脚本区（`tokenKey` 定义之后）新增免登逻辑：

```js
  const inDingtalk = typeof window.dd !== 'undefined' && !!(window.dd && window.dd.runtime);

  async function dingtalkLogin() {
    const cfg = await json('/auth/config');           // { corp_id }
    if (!cfg.corp_id) throw new Error('corp_id 未配置');
    const code = await new Promise((resolve, reject) => {
      window.dd.runtime.permission.requestAuthCode({
        corpId: cfg.corp_id,
        onSuccess: (res) => resolve(res.code),
        onFail: reject,
      });
    });
    const result = await json('/auth/dingtalk', {
      method: 'POST',
      body: JSON.stringify({ code }),
    });
    localStorage.setItem(tokenKey, result.access_token);
    localStorage.setItem('portal-user', JSON.stringify(result.user));
    return result.user;
  }

  function currentUser() {
    try { return JSON.parse(localStorage.getItem('portal-user') || 'null'); }
    catch { return null; }
  }
  const canViewData = () => {
    const u = currentUser();
    return !!u && u.role === 'admin';
  };
```

- [ ] **Step 2: 用免登替换密码框（删除 `bindLoginPanel` / `ensureLoginPanel`，改门禁提示）**

- 删除 `bindLoginPanel` 与 `ensureLoginPanel` 两个函数（约 1335–1393 行）。
- 新增渲染「数据端无权限 / 非钉钉环境」提示的函数，替换原 `ensureLoginPanel` 调用点（`loadDataViews`/`refreshSummaryEvents`/`ai_query` 的 `error.unauthorized` 分支）：

```js
  function renderDataGate(message) {
    ['data-view', 'summary-view'].forEach((viewId) => {
      const view = document.getElementById(viewId);
      if (!view) return;
      let panel = view.querySelector('.data-gate-panel');
      if (!panel) {
        view.insertAdjacentHTML('afterbegin',
          `<section class="data-gate-panel data-panel rounded-[16px] p-8 mb-8">
             <div class="flex items-center space-x-3">
               <div class="w-10 h-10 rounded-xl bg-primary-fixed flex items-center justify-center">
                 <span class="material-symbols-outlined text-primary">lock</span>
               </div>
               <div><h2 class="font-headline-md text-headline-md">无法查看数据</h2>
               <p class="data-gate-msg text-label-md text-on-surface-variant/60"></p></div>
             </div>
           </section>`);
        panel = view.querySelector('.data-gate-panel');
      }
      panel.querySelector('.data-gate-msg').textContent = message;
      panel.classList.remove('hidden');
    });
  }
```

把 `loadDataViews`、`refreshSummaryEvents`、`ai_query` 里 `error.unauthorized` 分支的
`ensureLoginPanel()` 替换为
`renderDataGate('你不是管理员，无权查看统计与明细，请联系超管开通')`。

- [ ] **Step 3: boot 串起来（免登 → 按角色加载）**

把 `bootRealData` 改为先免登再按角色加载：

```js
  async function bootRealData() {
    if (booted) return;
    booted = true;
    if (!inDingtalk) {
      renderDataGate('请在钉钉客户端内打开本页面以查看数据');
      try { await loadTools(); } catch (e) { console.warn(e); }
      return;
    }
    try {
      await dingtalkLogin();
    } catch (e) {
      console.warn('钉钉免登失败', e);
      renderDataGate('钉钉免登失败，请重新打开页面');
    }
    try { await loadTools(); } catch (e) { console.warn(e); }
    if (canViewData()) {
      await loadDataViews();
      renderAdminPanelIfSuperadmin();
    } else {
      renderDataGate('你不是管理员，无权查看统计与明细，请联系超管开通');
    }
  }
```

> 注：工具门户首页（`loadTools`）对所有免登成功的员工可见；统计/明细仅 `canViewData()`。

- [ ] **Step 4: 超管「成员权限管理」面板**

新增（仅超管渲染，挂在数据汇总页或单独一处；这里挂 `summary-view` 顶部）：

```js
  async function renderAdminPanelIfSuperadmin() {
    const u = currentUser();
    if (!u || !u.is_superadmin) return;   // 见下方说明：user 需带 is_superadmin
    const view = document.getElementById('summary-view');
    if (!view || view.querySelector('.admin-manage-panel')) return;
    let admins = [];
    try { admins = await json('/auth/admins'); } catch (e) { return; }
    const rows = admins.map((a) => `
      <tr class="border-b border-outline-variant/10">
        <td class="py-2">${escapeHtml(a.display_name || a.dingtalk_userid || a.user_id)}</td>
        <td class="py-2">${a.role === 'admin' ? '管理员' : '员工'}</td>
        <td class="py-2 text-right">
          <button data-uid="${escapeHtml(a.user_id)}" data-next="${a.role === 'admin' ? 'user' : 'admin'}"
            class="admin-toggle bg-primary text-on-primary rounded-lg px-3 py-1 text-xs">
            ${a.role === 'admin' ? '取消管理员' : '设为管理员'}</button>
        </td>
      </tr>`).join('');
    view.insertAdjacentHTML('afterbegin', `
      <section class="admin-manage-panel data-panel rounded-[16px] p-6 mb-8">
        <h2 class="font-headline-md text-headline-md mb-4">成员权限管理</h2>
        <table class="w-full text-sm"><tbody>${rows}</tbody></table>
      </section>`);
    view.querySelectorAll('.admin-toggle').forEach((btn) => {
      btn.addEventListener('click', async () => {
        await json(`/auth/admins/${btn.dataset.uid}`, {
          method: 'POST', body: JSON.stringify({ role: btn.dataset.next }),
        });
        view.querySelector('.admin-manage-panel').remove();
        renderAdminPanelIfSuperadmin();
      });
    });
  }
```

> **依赖：** 前台需要知道当前用户是否超管。`TokenResponse.user`（`UserPublic`）目前不含 `is_superadmin`。在 Task 6 完成后补一小步：给 `UserPublic` 增加 `is_superadmin: bool = False`，并在 `public_user`/`dingtalk_login` 返回时带上（超管为 True）。**实现 Task 11 前先回 Task 6 的 app.py 补这个字段并加一个断言测试**（见下方 Step 4b）。

- [ ] **Step 4b: 回补 UserPublic.is_superadmin（后端，TDD）**

Test（加到 `test_app.py`）：

```python
def test_dingtalk_login_marks_superadmin_in_user() -> None:
    client = viewer_client("user", superadmin=True)
    body = client.post("/auth/dingtalk", json={"code": "c"}).json()
    assert body["user"]["is_superadmin"] is True
    assert body["user"]["role"] == "admin"
```

实现：`UserPublic` 增加 `is_superadmin: bool = False`；`public_user` 改为接受可选超管标记，或在 `dingtalk_login` 里构造返回时显式带上。最小改法——`dingtalk_login` 返回改为：

```python
        user_public = public_user(account)
        return TokenResponse(
            access_token=token,
            expires_at=expires_at,
            user=user_public.model_copy(update={"is_superadmin": is_superadmin}),
        )
```

Run: `cd platform; pytest backend/ingestion/test_app.py -k superadmin_in_user -q` → PASS

- [ ] **Step 5: 提交**

```bash
git add platform/apps/web/index.html platform/backend/ingestion/app.py platform/backend/ingestion/test_app.py
git commit -m "feat(web): 钉钉免登 + 按角色显隐数据端 + 超管成员管理面板"
```

---

## Task 12: 浏览器预览验证（人工/preview 工具）

**Files:** 无（验证）

- [ ] **Step 1: 启动前台**：确保 `start-platform.cmd` 在跑（PG+API+web 5174）。
- [ ] **Step 2: 非钉钉环境**：普通 Chrome 开 `http://192.168.50.27:5174/` → 工具门户可见；切到数据页应显示「请在钉钉客户端内打开」，**无密码框**。
- [ ] **Step 3: 钉钉环境（超管）**：钉钉里打开门户 → 无登录动作直接进；数据页可见统计+明细；数据汇总页顶部有「成员权限管理」面板。
- [ ] **Step 4: 设一名员工为 admin → 该员工钉钉打开后能看数据，但无管理面板。**
- [ ] **Step 5: 普通员工**：钉钉打开 → 工具+历史可用；数据页显示「无权查看」。

---

## Task 13: 文档与契约同步（CLAUDE.md §3/§5）

**Files:**
- Modify: `PROJECT_PLAN.md`（决策记录）
- Modify: `platform/docs/architecture-架构与原则.md`（入口门户与账号体系）
- Modify: `platform/docs/execution-plan-执行计划与技术栈.md`（新增环境变量说明）
- Modify: `platform/.env.example`
- Modify: `开发日志.md`、`CHANGELOG.md`
- Modify: 记忆 `sensitive-policy-read-side-deferred`

- [ ] **Step 1: 决策记录**：在 `PROJECT_PLAN.md` 追加一条——把决策 #44「平台账号密码门禁」更正为「钉钉免登 + 三层角色门禁（超管配置认定 / admin DB 认定 / 普通员工只用工具）」，记 P2 密码登录正式退役。
- [ ] **Step 2: 架构正文**：更新「入口门户与账号体系」描述为免登 + 角色门禁；移除密码登录表述。
- [ ] **Step 3: 执行计划 + .env.example**：新增 `BOOTSTRAP_ADMIN_DINGTALK_USERID`、`DINGTALK_CORP_ID` 说明（复用 `DINGTALK_CLIENT_ID/SECRET`）。
- [ ] **Step 4: 设计稿归档标注**：在 `platform/docs/specs/2026-06-12-钉钉免登数据门禁-design.md` 顶部状态改为「已实现」。
- [ ] **Step 5: 开发日志 + CHANGELOG**：开发日志追加一条；CHANGELOG 记一条能力变更（免登 + 数据端角色门禁 + 密码退役，破坏性：移除 `/auth/login`）。
- [ ] **Step 6: 记忆更新**：把 `sensitive-policy-read-side-deferred` 改为「读取侧已落钉钉免登 + admin 门禁；部门级分级仍待定」。
- [ ] **Step 7: 机器闸门全绿**：

```
cd platform; ruff format --check .; ruff check .; mypy .; pytest -q
```
Expected: 全绿（格式/lint/类型/测试）。死代码检查确认无 `/auth/login`、`authenticate`、`bindLoginPanel`、`ensureLoginPanel` 残留。

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "docs: 钉钉免登数据门禁落地，同步决策/架构/执行计划/日志/CHANGELOG"
```

---

## 自检（写完计划对照 spec）

- ✅ spec §3 角色模型 → Task 1/4/6/7（is_superadmin、超管配置、免登判定、门禁）
- ✅ spec §4 链路 → Task 3/6/11（免登 code→userid→token；前台 boot）
- ✅ spec §5.1 免登能力 → Task 3
- ✅ spec §5.2 三个接口 → Task 6（/auth/dingtalk）、Task 8（/auth/admins）、Task 9 间接（config）
- ✅ spec §5.3 claims+门禁 → Task 1/7
- ✅ spec §5.4 删密码登录 → Task 10
- ✅ spec §6 配置（corpId/bootstrap/复用密钥）→ Task 4/9/13
- ✅ spec §7 前台 → Task 11
- ✅ spec §8 错误处理 → Task 6（免登失败 401 / 未同步 403）、Task 7（非 admin 403）
- ✅ spec §9 测试 → 各 Task 的 TDD 步骤，mock 钉钉不打真云
- ✅ spec §10 落地同步 → Task 13
