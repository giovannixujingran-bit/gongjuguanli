# 对话交接快照（2026-06-04）

> 这是一份**一次性交接快照**，给接手的人 / AI 快速进入上下文用，**不是 SSOT、不纳入文档体系**，可随时删除。
> 权威信息以 `platform/docs/`（规范）+ `PROJECT_PLAN.md`（决策记录）+ `开发日志.md`（流水）为准。

---

## 1. 项目一句话

公司**局域网内**的统一平台：把各类工具（自写 / 开源自部署 / 黑盒）的**每一次使用**按统一数据契约汇总入库，做**成本 / ROI / 使用率 / 质量**四类分析。由**两个前端（使用端·使用者门户 / 数据端·后台分析台）+ 共享后端**组成。

- 部署形态：`docker-compose` 单点部署，**通过局域网链接给人用，不是分发副本**。威胁模型据此：不做写入侧鉴权，重点管读取侧（谁能看原文）。
- 技术栈（已锁定）：后端 Python 3.11+ / FastAPI / Pydantic v2，DB PostgreSQL（metadata 用 JSONB），前端 React+Vite+TS，校验用 JSON Schema（唯一源生成模型）。

**当前阶段**：Phase 2A/2B 最小实现已完成；**下一步 = 真实 PostgreSQL 冒烟 → Phase 2C 真实工具低风险试点**。

---

## 2. 现在的文档结构（重要：刚做完一次大迁移）

**所有规范文档现在只有一个 SSOT 位置：`platform/docs/`**（原 `规划/` 目录已全量搬入并**退役删除**，见决策 #30）。

| 文档 | 内容 |
|---|---|
| `platform/docs/architecture.md` | 架构与原则、五层架构、入口门户与账号体系 |
| `platform/docs/execution-plan.md` | 分阶段计划、技术栈、目录结构、交接命令 |
| `platform/docs/code-standards.md` | 工程纪律、机器闸门（写代码前必读） |
| `platform/docs/schema.md` | 数据契约 v0.2（三圈字段），人读版 |
| `platform/docs/contract.md` | 接入契约（给工具方看） |
| `platform/docs/registry.md` | 工具注册表（tool_id 来源 + 门户展示字段） |
| `platform/docs/portal.md` | 使用端门户规划 |
| `platform/shared/schema/event.schema.json` | **机器可校验唯一源**，后端/前端模型由它生成 |
| 根 `PROJECT_PLAN.md` | 总纲 + 决策记录（#1–#31） |
| 根 `开发日志.md` / `CHANGELOG.md` | 过程流水 / 能力摘要 |
| 根 `CLAUDE.md` | 文档维护守则（动任何 .md 前先读） |

**建议阅读顺序**：CLAUDE.md → PROJECT_PLAN.md（决策记录）→ docs/architecture.md → docs/schema.md + event.schema.json → docs/contract.md → 现有代码。

---

## 3. 代码现状（platform/）

已实现（✅）/ 占位（⬜）：

- ✅ `backend/ingestion/app.py`：接入 API。`POST /events`（契约校验 → 幂等落库 → 匿名兜底 → 未知 schema_version 告警不拒收）；`/auth/users`(**需 admin**)、`/auth/login`、`/auth/verify`、`/auth/me`。
- ✅ `backend/storage/events.py`：幂等 INSERT（`ON CONFLICT DO NOTHING`）。
- ✅ `backend/storage/migrations/0001_init.sql`：三张表（usage_event / tool_registry / user_account）。
- ✅ `backend/auth/`：PBKDF2 密码哈希、账号 repository、HMAC token。
- ✅ `collection/sdk/tracker.py`：参考 SDK（事件构造、上报、本地 buffer + **成功时自动重发**、token 归一化）。`demo_tool.py` 演示。
- ✅ `shared/schema_version.py`：契约版本**单一来源**（`CURRENT_SCHEMA_VERSION` / `SUPPORTED_SCHEMA_VERSIONS`）。
- ✅ `shared/contracts/`：由 schema 生成的 Pydantic/TS 模型（自动生成，勿手改）。
- ✅ `tools/`：`smoke_db.py`（真实 DB 冒烟）、`seed_admin.py`（引导首个 admin）、`generate_contracts.py`（生成模型）。
- ✅ `integrations/_template/`：真实工具试点模板。
- ⬜ `collection/relay/`：兜底转发服务——**只有设计稿**（`README.md`，待确认 4 点后实现，见 §5）。
- ⬜ `backend/analytics/`（分析层 Phase 3）、`apps/`（两个前端 Phase 4）：占位。

---

## 4. 本次对话改了什么（2026-06-04）

围绕「数据传输 / 存储 / 多方协作」做整体复查并落地整改：

1. **修易误导处**：SDK `async_report` → `background_send`（与契约「异步两段式」区分）；建表 SQL `tool_id` 的「FK」注释改为「逻辑引用、不建强制外键」；schema.md `result_quality` 收敛为 float 并提示 enum 形态走 metadata。
2. **契约版本单一来源**：新增 `shared/schema_version.py`，SDK/冒烟不再硬编码 `"v0.2"`；接入层对未知版本**告警但不拒收**。
3. **SDK 重试自愈**：上报成功顺带自动 flush 积压 buffer + 加锁；接入模板 buffer 路径按工具隔离。
4. **安全收紧**：`POST /auth/users` 改为**需 admin token**，首个 admin 由 `tools/seed_admin.py` 离线引导（决策 #31）。
5. **文档大迁移**：`规划/` 5 份规范 + mockup 全部搬入 `platform/docs/` 并统一英文名，`规划/` 退役删除，全仓链接重定向（决策 #30）。

新增决策 **#30**（全量迁移）、**#31**（admin 门禁）。

---

## 5. 待办 / 待人工拍板（接手重点）

**待用户确认设计后才能写代码：**
- **relay（兜底转发，Phase 2D）** 设计稿在 `platform/collection/relay/README.md`，4 个待定点：① 首版支持哪些 provider（建议 OpenAI 兼容 + Claude Messages）；② 流式 SSE 是否进首版；③ `key/路由 → tool_id` 映射存注册表还是 relay 配置；④ OpenClaw 实际怎么改 base_url。

**长期「待定，需人工确认」（不要替用户拍板）：**
- 敏感内容策略 + 读取侧权限分级（谁能看 input/output 原文）。试点首轮**不记原文**绕过。
- ROI 价值信号（用什么代表「产出」）。**ROI 算式现在先不做**，只保证 schema/存储能兼容任意将来要买的数据（现有弹性选填 + metadata 已满足）。

**已规划待实现（Phase 2.5 接入工程化）：**
- `tools/validate_payload.py`（接入方自助离线校验）、`tools/register_tool.py`（tool_id 发放通道 + 按团队命名）、接入层连接池（`psycopg_pool`）。

**标准化欠账（长期可维护性，非 bug）：**
- DB 迁移无 runner（建议 Alembic）；Python 版本三处不一致（本机 3.13 / vendored cp312 / 容器 3.11，应统一）；配置可改用 `pydantic-settings`；自研 HMAC token 可换 PyJWT；缺 Dockerfile。

---

## 6. ⚠️ 两个必须知道的坑

1. **本机跑不全测试**：`platform/.pydeps` / `.toolprefix` 里的编译扩展是 **Python 3.12（cp312）**，本机只有 **3.13**，ABI 不匹配 → **pytest 和 import-linter 在本机跑不起来**（只能跑 ruff / vulture / 单文件 mypy）。**需到装了 Python 3.12 的环境补跑** pytest + import-linter。本次所有代码改动只做了静态闸门验证，pytest 未在本机跑。

2. **工作区有未提交改动**：本次大量改动（含文档迁移）**尚未 git commit**。迁移过程中曾因「对未提交改动的文件做 git mv + checkout」丢失过内容（已恢复）。**强烈建议接手第一步先 commit**固化当前状态，且**移动有未提交改动的文件前务必先 commit**。

---

## 7. 一句话给接手 AI

读 `CLAUDE.md` + `PROJECT_PLAN.md` 决策记录，规范全在 `platform/docs/`；下一步是真实 DB 冒烟 + Phase 2C 试点；relay 和敏感策略 / ROI 等**待用户拍板前别擅自实现**；本机别指望跑 pytest（Python 版本不匹配）。
