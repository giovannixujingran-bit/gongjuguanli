# 执行计划与技术栈

> 给**建平台的团队**看的：分几步做、用什么技术、目录怎么放、怎么交接给 AI 部署。
> 每完成一个 Phase 会更新本文件。

---

## 一、分阶段执行计划

> 严格按地基优先。Phase 0 不定死，后面全是返工。

- **Phase 0 — 地基：Schema + 契约**
  产出 [数据契约](../共享层/数据契约.md) 与 [接入契约](../数据端/接入契约.md) 的字段表、JSON Schema 校验定义、字段文档。这是后续一切的依据。

- **Phase 1 — 存储层（共享）**
  建 PostgreSQL 库与统一事件表（metadata 用 JSONB）、[工具注册表](../共享层/工具注册表.md)（**含门户展示字段**：category / display_name / description / icon / thumbnail / launch_url / sort_weight / enabled）、**用户账号表**（账号 / 密码哈希 / user_id / team_id / 角色）；建接入 API（接收一条流水 → 校验契约 → 落库）。三张表是两个前端共用的共享数据。

- **Phase 2 — 采集层**
  a) 写参考 SDK（**工具自报的便捷封装**，主路：包装 LLM 调用或按钮触发点，自动抠 `usage` 并**归一化**为统一字段、接住门户注入的 `user_id` 回传、异步上报，中转站地址作为可配置项隔离在内部）。**异步上报须带本地缓冲 + 重试队列**：平台不可达时先落本地、稍后重试，避免丢数据——而「失败/超时」恰常与平台不可达同时发生，最该记的时候最易丢。重试靠 `record_id` 幂等去重，不会重复计费；
  b) 写本地转发服务（转发请求给中转站 + 旁路记一条流水），供 OpenClaw / 黑盒工具改指向。

- **Phase 3 — 分析层**
  基于原始流水计算四类结论（成本 / ROI / 采纳率 / 质量）。算式全部在此层，与采集解耦。

- **Phase 4 — 展示层（两个前端）**
  - **使用端 · 使用者门户**（`apps/user-portal`）：分类卡片、排序（偏好/频次/时间）、AI 工具推荐、登录入口（打开工具时注入 `user_id`）。详见 [使用端-工具门户](../使用端/工具门户.md)。
  - **数据端 · 后台分析台**（`apps/admin-dashboard`）：明细查询 + 四类分析看板 + 接入管理页 + 账号管理。**后台看数据时可加一个「AI 数据分析」助手**（接 LLM API 对看板数据做自然语言分析）——**待定，需人工确认，后置实现**。

- **Phase 5 — 接入存量工具**
  自写 / 可改代码的工具走自报（引 SDK 或自行上报，并接住门户注入的 `user_id`）；OpenClaw 改 base URL 指向转发服务；黑盒工具发独立 key 或走转发兜底。每个接入工具在 `integrations/` 下放一份接入适配/配置（工具本体仍各自独立部署，不进本仓库）。

---

## 二、建议的技术栈与目录结构

技术栈**已锁定**（详见 [代码规范](代码规范.md)，换栈需在决策记录立项）：

- 后端 / 接入 API / 分析层：Python 3.11+ + FastAPI + Pydantic v2
- 数据库：PostgreSQL（metadata = JSONB，天然适配自由口袋）
- 转发服务：默认 Python（httpx + SSE）；流式代理嫌费劲时可单独用 Node 实现这一个组件
- 校验：JSON Schema（`shared/schema/event.schema.json` 为唯一源，后端/前端模型均由它生成）
- 前端（两个）：React + Vite + TypeScript（图表用 Recharts / ECharts）
- 部署：docker-compose（局域网内一键起）

建议目录结构（这是**最终代码仓库**的结构，与本规划目录 `规划/` 不同；按「两个前端 + 共享后端 + 集成层」组织）：

```
platform/
├── README.md
├── docs/                        # 部署后承接 SSOT 的落地文档（schema.md / contract.md / architecture.md）
├── shared/                      # 共享层：两端共用的数据与契约
│   ├── schema/event.schema.json #   机器可校验的 JSON Schema（唯一源，Phase 0）
│   ├── contracts/               #   由 schema 生成的 py/ts 模型（标「自动生成，勿手改」）
│   └── registry/                #   工具注册表定义/迁移（含门户展示字段）
├── backend/                     # 数据端共享后端
│   ├── ingestion/               #   接入 API（Phase 1）
│   ├── storage/migrations/      #   建表 SQL（事件表 + 注册表 + 用户账号表，Phase 1）
│   ├── analytics/               #   分析层（Phase 3）
│   └── auth/                    #   账号体系（Phase 1）
├── collection/                  # 采集层
│   ├── sdk/                     #   参考 SDK（Phase 2a）
│   └── relay/                   #   本地转发服务（Phase 2b）
├── integrations/                # 「存放各个工具」：每个工具一子目录（接入适配/配置）
│   └── _template/               #   接入模板（新工具照抄）
├── apps/                        # 两个前端
│   ├── user-portal/             #   使用端 · 使用者门户（Phase 4）
│   └── admin-dashboard/         #   数据端 · 后台分析台（Phase 4）
└── docker-compose.yml
```

> **当前进度**：`platform/` 的**目录骨架 + 各目录 README 占位已建好**，但**尚未**生成 `event.schema.json`、建表 SQL、落地文档——故 SSOT 仍在 `规划/`，未冻结。生成上述 Phase 0 实质产出时，按 [CLAUDE.md](../../CLAUDE.md) 的 SSOT 生命周期，`规划/` 才整体冻结、SSOT 转移到 `platform/docs/`。

---

## 三、交接命令（可直接粘贴给 AI 执行）

> 这是**完整 Phase 0** 的交接命令（执行它会生成 schema/建表 SQL/落地文档，从而**触发 `规划/` 冻结**）。把下面整段连同 `规划/` 下的全部规范一起交给 AI。本阶段只搭脚手架与产出 Phase 0 文档，**不实现完整业务逻辑**。

```
请阅读 规划/ 目录下的全部规范文档（总则/架构与原则、总则/执行计划、总则/代码规范、
共享层/数据契约、共享层/工具注册表、数据端/接入契约、使用端/工具门户），以及根目录 CLAUDE.md。
照其中的架构、目录结构与代码规范，在 platform/ 子目录里产出 Phase 0 实质内容
（骨架与各目录 README 已存在，本轮在其上填 Phase 0 产出，不要超出以下范围）：

【要做】
1. 完成 Phase 0 的三份产出：
   - platform/docs/schema.md —— 按 共享层/数据契约 写出完整字段文档（三圈 + 弹性选填 + 敏感策略说明），
     含 schema_version / conversation_id / cost_source 与 token 归一化说明、敏感策略「待定，需人工确认」小节；
     user_id/team_id 为「门户注入，否则匿名」、input_content/output_content 为通用选填（不限大模型）。
   - platform/shared/schema/event.schema.json —— 把 共享层/数据契约 翻译成可校验的 JSON Schema，
     正确标注必填/选填、类型、枚举（含 status、cost_source 枚举值）。
   - platform/docs/contract.md —— 按 数据端/接入契约 写出给工具方看的接入契约说明，含「硬性必填 / 弹性选填」分级、
     tool_id 由平台分配、token 归一化要求、流式必须开 usage、失败/超时必须上报、异步两段式、兜底通道说明。
2. 写 platform/README.md，概述项目目标、三条核心原则、五层架构、两个前端 + 共享后端、三类工具接入方式（源自 总则/架构与原则）。
3. 写 platform/docker-compose.yml 骨架（PostgreSQL + 接入 API 占位 + 两个前端占位），可启动即可，服务内部逻辑留空。
4. platform/backend/storage/migrations/ 下写出建表 SQL：
   - 统一事件表：含 数据契约 全部字段（圈一/二/三），schema_version、conversation_id、ingested_at、cost_source 不要漏；
     metadata 用 JSONB；在 tool_id、conversation_id、start_time、status 上建合理索引。
   - 工具注册表：按 共享层/工具注册表 建表，含接入字段 + 门户展示字段（category/display_name/description/icon/thumbnail/launch_url/sort_weight/enabled）。
   - 用户账号表：账号、密码哈希、user_id、team_id、角色（密码只存哈希，不存明文）。
   - 不要预先建价格表（后续阶段再定，仅在注释里留位说明）。
5. 按 总则/代码规范 搭好「机器闸门」配置骨架（先有架子、规则可后续收紧）：
   - Python：pyproject.toml 配 ruff / mypy / vulture / import-linter / pytest 的基础配置。
   - 前端两个 app 占位目录：eslint + prettier + tsc + knip 的基础配置。
   - 根目录 .pre-commit-config.yaml + 一个最简 CI 配置（跑上述检查）。
   - schema→模型的代码生成命令留好（datamodel-code-generator / json-schema-to-typescript），生成物标注「自动生成，勿手改」。
   - 闸门强度先放在「原型档」（宽松能跑），不要一上来拧到 strict。

【不要做】
- 不要实现 SDK、转发服务、分析层、两个前端的具体业务逻辑（留空目录 + README 占位即可）。
- 不要接入任何真实工具或真实中转站。
- 不要硬编码任何 API Key、密钥、中转站地址或 AI 推荐用的 LLM 地址。
- 不要替我决定敏感内容策略——在 platform/docs/schema.md 中留出该小节并标注「待定，需人工确认」。

【完成后】
列出你创建的全部文件清单，指出哪些是 Phase 0 已完成的实质产出、哪些是后续阶段的占位；
并提醒：按 CLAUDE.md，此时应把 规划/ 冻结归档、SSOT 转移到 platform/docs/。
```
