# 执行计划与技术栈

> 给**建平台的团队**看的：分几步做、用什么技术、目录怎么放、怎么交接给 AI 部署。
> 每完成一个 Phase 会更新本文件。

---

## 一、分阶段执行计划

> 严格按地基优先。Phase 0 不定死，后面全是返工。

- **Phase 0 — 地基：Schema + 契约**
  产出 [数据契约](../共享层/数据契约.md) 与 [接入契约](../数据端/接入契约.md) 的字段表、JSON Schema 校验定义、字段文档。这是后续一切的依据。

- **Phase 1 — 存储层（共享）**
  建 PostgreSQL 库与统一事件表（metadata 用 JSONB）、[工具注册表](../共享层/工具注册表.md)（**含门户展示字段**：category / display_name / description / icon / thumbnail / launch_url / sort_weight / enabled）、**用户账号表**（账号 / 密码哈希 / user_id / team_id / 角色）；建接入 API（接收一条流水 → 校验契约 → 落库）。三张表是两个前端共用的共享数据。

- **Phase 2 — 采集层**
  a) 写参考 SDK（**工具自报的便捷封装**，主路：包装 LLM 调用或按钮触发点，自动抠 `usage` 并**归一化**为统一字段、接住门户注入的 `user_id` 回传、异步上报，中转站地址作为可配置项隔离在内部）。**异步上报须带本地缓冲 + 重试队列**：平台不可达时先落本地、稍后重试，避免丢数据——而「失败/超时」恰常与平台不可达同时发生，最该记的时候最易丢。重试靠 `record_id` 幂等去重，不会重复计费；
  b) 写本地转发服务（转发请求给中转站 + 旁路记一条流水），供 OpenClaw / 黑盒工具改指向。

- **Phase 3 — 分析层**
  基于原始流水计算四类结论（成本 / ROI / 采纳率 / 质量）。算式全部在此层，与采集解耦。

- **Phase 4 — 展示层（两个前端）**
  - **使用端 · 使用者门户**（`apps/user-portal`）：分类卡片、排序（偏好/频次/时间）、AI 工具推荐、登录入口（打开工具时注入 `user_id`）。详见 [使用端-工具门户](../使用端/工具门户.md)。
  - **数据端 · 后台分析台**（`apps/admin-dashboard`）：明细查询 + 四类分析看板 + 接入管理页 + 账号管理。**后台看数据时可加一个「AI 数据分析」助手**（接 LLM API 对看板数据做自然语言分析）——**待定，需人工确认，后置实现**。

- **Phase 5 — 接入存量工具**
  自写 / 可改代码的工具走自报（引 SDK 或自行上报，并接住门户注入的 `user_id`）；OpenClaw 改 base URL 指向转发服务；黑盒工具发独立 key 或走转发兜底。每个接入工具在 `integrations/` 下放一份接入适配/配置（工具本体仍各自独立部署，不进本仓库）。

---

## 二、建议的技术栈与目录结构

技术栈**已锁定**（详见 [代码规范](代码规范.md)，换栈需在决策记录立项）：

- 后端 / 接入 API / 分析层：Python 3.11+ + FastAPI + Pydantic v2
- 数据库：PostgreSQL（metadata = JSONB，天然适配自由口袋）
- 转发服务：默认 Python（httpx + SSE）；流式代理嫌费劲时可单独用 Node 实现这一个组件
- 校验：JSON Schema（`shared/schema/event.schema.json` 为唯一源，后端/前端模型均由它生成）
- 前端（两个）：React + Vite + TypeScript（图表用 Recharts / ECharts）
- 部署：docker-compose（局域网内一键起）

建议目录结构（这是**最终代码仓库**的结构，与本规划目录 `规划/` 不同；按「两个前端 + 共享后端 + 集成层」组织）：

```
platform/
├── README.md
├── docs/                        # 部署后承接 SSOT 的落地文档（schema.md / contract.md / architecture.md）
├── shared/                      # 共享层：两端共用的数据与契约
│   ├── schema/event.schema.json #   机器可校验的 JSON Schema（唯一源，Phase 0）
│   ├── contracts/               #   由 schema 生成的 py/ts 模型（标「自动生成，勿手改」）
│   └── registry/                #   工具注册表定义/迁移（含门户展示字段）
├── backend/                     # 数据端共享后端
│   ├── ingestion/               #   接入 API（Phase 1）
│   ├── storage/migrations/      #   建表 SQL（事件表 + 注册表 + 用户账号表，Phase 1）
│   ├── analytics/               #   分析层（Phase 3）
│   └── auth/                    #   账号体系（Phase 1）
├── collection/                  # 采集层
│   ├── sdk/                     #   参考 SDK（Phase 2a）
│   └── relay/                   #   本地转发服务（Phase 2b）
├── integrations/                # 「存放各个工具」：每个工具一子目录（接入适配/配置）
│   └── _template/               #   接入模板（新工具照抄）
├── apps/                        # 两个前端
│   ├── user-portal/             #   使用端 · 使用者门户（Phase 4）
│   └── admin-dashboard/         #   数据端 · 后台分析台（Phase 4）
└── docker-compose.yml
```

> **当前进度（Phase 0 已完成）**：`platform/` 已产出 Phase 0 实质内容——`shared/schema/event.schema.json`（唯一源）、`docs/schema.md`+`docs/contract.md`、`backend/storage/migrations/0001_init.sql`（事件表+注册表+账号表）、`README`/`docker-compose`/`.env.example`、机器闸门配置骨架（pyproject + 两前端 lint/类型/死代码 + pre-commit + CI + 代码生成脚本）。代码区已入 Git。
> **SSOT 部分冻结**（决策 #26 取 (a)）：数据契约 → `platform/docs/schema.md`+`event.schema.json`、接入契约 → `platform/docs/contract.md` **已冻结转移**；本文件与架构/代码规范/工具注册表/工具门户 5 份**仍活在 `规划/`**。改文档前认这条线（见 [CLAUDE.md](../../CLAUDE.md) §1）。
> **下一步 = Phase 1**，交接命令见 §三。

---

## 三、交接命令（可直接粘贴给 AI 执行）

> **Phase 0 已完成**（见 §二「当前进度」与 [开发日志](../../开发日志.md)）。下面是 **Phase 1（存储层 + 接入 API）** 的交接命令，可直接整段粘贴给执行 AI。
>
> ⚠️ **读单已变（务必照新版）**：SSOT 已部分转移到 `platform/`。数据契约 / 接入契约的**真源在 `platform/`**，`规划/` 里那两份带 🧊「已冻结」标记、**只读不改**。所以读单不再是「只读 `规划/`」，必须连 `platform/` 一起读。

```
你要接续一个已完成 Phase 0 的项目，实现 Phase 1（存储层 + 接入 API）。先按顺序读全这些，再动手：

【必读 —— 工程治理与现状】
- 根目录 CLAUDE.md（最高优先级：文档维护守则、SSOT 部分冻结的当前状态、改完自检清单）
- PROJECT_PLAN.md（决策记录，尤其 #1–#6、#13、#14、#26；文档地图）
- 开发日志.md（最近两条 = Phase 0 做了什么 + 部分冻结）

【必读 —— 仍活在 规划/ 的规范（这 5 份是 SSOT，照常参照）】
- 规划/总则/架构与原则.md、规划/总则/执行计划.md、规划/总则/代码规范.md
- 规划/共享层/工具注册表.md、规划/使用端/工具门户.md

【必读 —— 已冻结、SSOT 在 platform/（只读不改 规划/ 原件，要改改 platform/）】
- platform/docs/schema.md + platform/shared/schema/event.schema.json  ← 数据契约真源（v0.2）
- platform/docs/contract.md                                            ← 接入契约真源
- platform/README.md、platform/backend/storage/migrations/0001_init.sql、platform/pyproject.toml、platform/.env.example

【本轮要做 —— Phase 1：存储层 + 接入 API】
1. 代码生成：跑 platform/scripts/gen-models（.sh 或 .ps1），从 event.schema.json 生成
   platform/shared/contracts/ 下的 Pydantic v2 模型与 TS 类型；生成物文件头须含「自动生成，勿手改」，
   且被 lint/类型检查/格式化排除（配置已留位）。各层引用生成模型，绝不手写字段。
2. 存储层接线：让 docker-compose 起 Postgres 时自动执行 0001_init.sql 建三表（事件表/注册表/账号表），
   可连。DB 口令/URL 走 .env，零硬编码。
3. 接入 API（platform/backend/ingestion，FastAPI）：实现统一上报端点——
   - 接收一条流水 → 用生成的 Pydantic 模型 + event.schema.json 校验契约，不合规当场打回（4xx）；
   - 圈一缺失即拒收；user_id 拿不到记 anonymous，绝不因此拒收（守数据契约「不阻断入库」）；
   - 按 record_id 幂等去重（重复上报不重复入库/计费）；ingested_at 服务端落库时盖章；
   - 落库进 usage_event，metadata 进 JSONB。
4. 账号表最小支撑（platform/backend/auth）：密码只存哈希（如 argon2/bcrypt），提供建账号/校验的内部函数即可；
   登录前端与读取侧权限细则不在本轮（仍待人工确认）。
5. 测试 + 拧紧闸门：给接入校验/幂等/落库写 pytest；把 pyproject 的 mypy 开 --strict、
   把 import-linter（分层）与 pytest 从 CI 的 continue-on-error 提为必过；本地 pre-commit install 跑通。

【不要做】
- 不实现采集 SDK / 转发服务（Phase 2）、分析层四类算式（Phase 3）、两个前端（Phase 4）、接真实工具（Phase 5）。
- 不接任何真实工具或真实中转站；不硬编码任何 Key/口令/中转站地址/LLM 地址（全走 .env）。
- 不替用户决定敏感内容策略、读取侧权限、ROI 价值信号、价格表——这些仍「待定，需人工确认」，保持占位。
- 不修改 规划/ 下带 🧊 的已冻结文档（数据契约、接入契约）；要改契约改 platform/，并按 CLAUDE.md §3
  四连动（升 schema_version + 决策记录 + event.schema.json + 建表 SQL + 重跑代码生成）。

【完成后】
- 列出新增/修改的文件清单（区分实质实现 vs 仍占位）。
- 跑一遍机器闸门并贴结果（ruff/mypy/vulture/import-linter/pytest 全绿才算完成；红了修，不许加忽略糊弄）。
- 按 CLAUDE.md 在 开发日志.md 追加一条；有新取舍写进 PROJECT_PLAN 决策记录。
- 若把某份仍活在 规划/ 的规范也搬进了 platform/，给它加 🧊 冻结标记并更新 CLAUDE.md §1/§2/§3 指向。
```
